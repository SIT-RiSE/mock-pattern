[
  {
    "rawMockObjectId": 1,
    "variableName": "mockQueryRunner",
    "variableType": "QueryRunner",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\AsyncQueryRunnerTest.java"
    },
    "statements": [
      {
        "code": "final QueryRunner mockQueryRunner = mock(QueryRunner.class, // debug for Continuum\r\norg.mockito.Mockito.withSettings().verboseLogging());",
        "line": 370,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testInsertUsesGivenQueryRunner",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testInsertUsesGivenQueryRunner() throws Exception {\r\n    final QueryRunner mockQueryRunner = mock(QueryRunner.class, // debug for Continuum\r\n    org.mockito.Mockito.withSettings().verboseLogging());\r\n    runner = new AsyncQueryRunner(Executors.newSingleThreadExecutor(), mockQueryRunner);\r\n    runner.insert(\"1\", handler);\r\n    runner.insert(\"2\", handler, \"param1\");\r\n    runner.insert(conn, \"3\", handler);\r\n    runner.insert(conn, \"4\", handler, \"param1\");\r\n    // give the Executor time to submit all insert statements. Otherwise the following verify statements will fail from time to time.\r\n    TimeUnit.MILLISECONDS.sleep(50);\r\n    verify(mockQueryRunner).insert(\"1\", handler);\r\n    verify(mockQueryRunner).insert(\"2\", handler, \"param1\");\r\n    verify(mockQueryRunner).insert(conn, \"3\", handler);\r\n    verify(mockQueryRunner).insert(conn, \"4\", handler, \"param1\");\r\n}"
        }
      },
      {
        "code": "verify(mockQueryRunner).insert(\"1\", handler);",
        "line": 383,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testInsertUsesGivenQueryRunner",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testInsertUsesGivenQueryRunner() throws Exception {\r\n    final QueryRunner mockQueryRunner = mock(QueryRunner.class, // debug for Continuum\r\n    org.mockito.Mockito.withSettings().verboseLogging());\r\n    runner = new AsyncQueryRunner(Executors.newSingleThreadExecutor(), mockQueryRunner);\r\n    runner.insert(\"1\", handler);\r\n    runner.insert(\"2\", handler, \"param1\");\r\n    runner.insert(conn, \"3\", handler);\r\n    runner.insert(conn, \"4\", handler, \"param1\");\r\n    // give the Executor time to submit all insert statements. Otherwise the following verify statements will fail from time to time.\r\n    TimeUnit.MILLISECONDS.sleep(50);\r\n    verify(mockQueryRunner).insert(\"1\", handler);\r\n    verify(mockQueryRunner).insert(\"2\", handler, \"param1\");\r\n    verify(mockQueryRunner).insert(conn, \"3\", handler);\r\n    verify(mockQueryRunner).insert(conn, \"4\", handler, \"param1\");\r\n}"
        }
      },
      {
        "code": "verify(mockQueryRunner).insert(\"2\", handler, \"param1\");",
        "line": 384,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testInsertUsesGivenQueryRunner",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testInsertUsesGivenQueryRunner() throws Exception {\r\n    final QueryRunner mockQueryRunner = mock(QueryRunner.class, // debug for Continuum\r\n    org.mockito.Mockito.withSettings().verboseLogging());\r\n    runner = new AsyncQueryRunner(Executors.newSingleThreadExecutor(), mockQueryRunner);\r\n    runner.insert(\"1\", handler);\r\n    runner.insert(\"2\", handler, \"param1\");\r\n    runner.insert(conn, \"3\", handler);\r\n    runner.insert(conn, \"4\", handler, \"param1\");\r\n    // give the Executor time to submit all insert statements. Otherwise the following verify statements will fail from time to time.\r\n    TimeUnit.MILLISECONDS.sleep(50);\r\n    verify(mockQueryRunner).insert(\"1\", handler);\r\n    verify(mockQueryRunner).insert(\"2\", handler, \"param1\");\r\n    verify(mockQueryRunner).insert(conn, \"3\", handler);\r\n    verify(mockQueryRunner).insert(conn, \"4\", handler, \"param1\");\r\n}"
        }
      },
      {
        "code": "verify(mockQueryRunner).insert(conn, \"3\", handler);",
        "line": 385,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testInsertUsesGivenQueryRunner",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testInsertUsesGivenQueryRunner() throws Exception {\r\n    final QueryRunner mockQueryRunner = mock(QueryRunner.class, // debug for Continuum\r\n    org.mockito.Mockito.withSettings().verboseLogging());\r\n    runner = new AsyncQueryRunner(Executors.newSingleThreadExecutor(), mockQueryRunner);\r\n    runner.insert(\"1\", handler);\r\n    runner.insert(\"2\", handler, \"param1\");\r\n    runner.insert(conn, \"3\", handler);\r\n    runner.insert(conn, \"4\", handler, \"param1\");\r\n    // give the Executor time to submit all insert statements. Otherwise the following verify statements will fail from time to time.\r\n    TimeUnit.MILLISECONDS.sleep(50);\r\n    verify(mockQueryRunner).insert(\"1\", handler);\r\n    verify(mockQueryRunner).insert(\"2\", handler, \"param1\");\r\n    verify(mockQueryRunner).insert(conn, \"3\", handler);\r\n    verify(mockQueryRunner).insert(conn, \"4\", handler, \"param1\");\r\n}"
        }
      },
      {
        "code": "verify(mockQueryRunner).insert(conn, \"4\", handler, \"param1\");",
        "line": 386,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testInsertUsesGivenQueryRunner",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testInsertUsesGivenQueryRunner() throws Exception {\r\n    final QueryRunner mockQueryRunner = mock(QueryRunner.class, // debug for Continuum\r\n    org.mockito.Mockito.withSettings().verboseLogging());\r\n    runner = new AsyncQueryRunner(Executors.newSingleThreadExecutor(), mockQueryRunner);\r\n    runner.insert(\"1\", handler);\r\n    runner.insert(\"2\", handler, \"param1\");\r\n    runner.insert(conn, \"3\", handler);\r\n    runner.insert(conn, \"4\", handler, \"param1\");\r\n    // give the Executor time to submit all insert statements. Otherwise the following verify statements will fail from time to time.\r\n    TimeUnit.MILLISECONDS.sleep(50);\r\n    verify(mockQueryRunner).insert(\"1\", handler);\r\n    verify(mockQueryRunner).insert(\"2\", handler, \"param1\");\r\n    verify(mockQueryRunner).insert(conn, \"3\", handler);\r\n    verify(mockQueryRunner).insert(conn, \"4\", handler, \"param1\");\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 2,
    "variableName": "dataSource",
    "variableType": "DataSource",
    "isGlobal": true,
    "mockPatternLevel": 2,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\AsyncQueryRunnerTest.java",
      "className": "AsyncQueryRunnerTest"
    },
    "statements": [
      {
        "code": "@Mock\r\nDataSource dataSource;",
        "line": 53,
        "type": "CREATION",
        "locate": "Attribute",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "FieldDeclaration",
          "methodAnnotations": [
            "Mock"
          ]
        }
      },
      {
        "code": "when(dataSource.getConnection()).thenReturn(conn);",
        "line": 256,
        "type": "STUBBING",
        "locate": "@Before",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new AsyncQueryRunner(Executors.newFixedThreadPool(1), new QueryRunner(dataSource));\r\n}"
        }
      },
      {
        "code": "when(dataSource.getConnection()).thenReturn(null);",
        "line": 403,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testNullConnectionBatch",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test(expected = ExecutionException.class)\r\npublic void testNullConnectionBatch() throws Exception {\r\n    final String[][] params = { { \"unit\", \"unit\" }, { \"test\", \"test\" } };\r\n    when(dataSource.getConnection()).thenReturn(null);\r\n    runner.batch(\"select * from blah where ? = ?\", params).get();\r\n}"
        }
      },
      {
        "code": "when(dataSource.getConnection()).thenReturn(null);",
        "line": 411,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testNullConnectionQuery",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test(expected = ExecutionException.class)\r\npublic void testNullConnectionQuery() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(null);\r\n    runner.query(\"select * from blah where ? = ?\", handler, \"unit\", \"test\").get();\r\n}"
        }
      },
      {
        "code": "when(dataSource.getConnection()).thenReturn(null);",
        "line": 418,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testNullConnectionUpdate",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test(expected = ExecutionException.class)\r\npublic void testNullConnectionUpdate() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(null);\r\n    runner.update(\"select * from blah where ? = ?\", \"unit\", \"test\").get();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 3,
    "variableName": "conn",
    "variableType": "Connection",
    "isGlobal": true,
    "mockPatternLevel": 1,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\AsyncQueryRunnerTest.java",
      "className": "AsyncQueryRunnerTest"
    },
    "statements": [
      {
        "code": "@Mock\r\nConnection conn;",
        "line": 54,
        "type": "CREATION",
        "locate": "Attribute",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "FieldDeclaration",
          "methodAnnotations": [
            "Mock"
          ]
        }
      },
      {
        "code": "// make sure we closed the connection\r\nverify(conn, times(0)).close();",
        "line": 95,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodBatch",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Batch test cases\r\n//\r\nprivate void callGoodBatch(final Connection conn, final Object[][] params) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    final Future<int[]> future = runner.batch(conn, \"select * from blah where ? = ?\", params);\r\n    future.get();\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the connection\r\nverify(conn, times(1)).close();",
        "line": 107,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodBatch",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodBatch(final Object[][] params) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    final Future<int[]> future = runner.batch(\"select * from blah where ? = ?\", params);\r\n    future.get();\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the connection\r\nverify(conn, times(1)).close();",
        "line": 118,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodQuery() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(sql, handler, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(sql, handler).get();\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the connection\r\nverify(conn, times(2)).close();",
        "line": 127,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodQuery() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(sql, handler, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(sql, handler).get();\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n}"
        }
      },
      {
        "code": "runner.query(conn, sql, handler, \"unit\", \"test\").get();",
        "line": 136,
        "type": "REFERENCE",
        "locate": "Other Methods",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Query test cases\r\n//\r\nprivate void callGoodQuery(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(conn, sql, handler, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(conn, sql, handler).get();\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the connection\r\nverify(conn, times(0)).close();",
        "line": 141,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Query test cases\r\n//\r\nprivate void callGoodQuery(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(conn, sql, handler, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(conn, sql, handler).get();\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "runner.query(conn, sql, handler).get();",
        "line": 145,
        "type": "REFERENCE",
        "locate": "Other Methods",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Query test cases\r\n//\r\nprivate void callGoodQuery(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(conn, sql, handler, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(conn, sql, handler).get();\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the connection\r\nverify(conn, times(0)).close();",
        "line": 150,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Query test cases\r\n//\r\nprivate void callGoodQuery(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(conn, sql, handler, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(conn, sql, handler).get();\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the connection\r\nverify(conn, times(1)).close();",
        "line": 160,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(\"update blah set unit = ?\", \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the connection\r\nverify(conn, times(2)).close();",
        "line": 169,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(\"update blah set unit = ?\", \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the connection\r\nverify(conn, times(3)).close();",
        "line": 177,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(\"update blah set unit = ?\", \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "runner.update(conn, sql, \"unit\", \"test\").get();",
        "line": 186,
        "type": "REFERENCE",
        "locate": "Other Methods",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(conn, sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(conn, sql, \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the connection\r\nverify(conn, times(0)).close();",
        "line": 190,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(conn, sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(conn, sql, \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "runner.update(conn, sql).get();",
        "line": 195,
        "type": "REFERENCE",
        "locate": "Other Methods",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(conn, sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(conn, sql, \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the connection\r\nverify(conn, times(0)).close();",
        "line": 199,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(conn, sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(conn, sql, \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "runner.update(conn, sql, \"test\").get();",
        "line": 204,
        "type": "REFERENCE",
        "locate": "Other Methods",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(conn, sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(conn, sql, \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the connection\r\nverify(conn, times(0)).close();",
        "line": 208,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(conn, sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(conn, sql, \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(dataSource.getConnection()).thenReturn(conn);",
        "line": 256,
        "type": "REFERENCE",
        "locate": "@Before",
        "isMockRelated": false,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new AsyncQueryRunner(Executors.newFixedThreadPool(1), new QueryRunner(dataSource));\r\n}"
        }
      },
      {
        "code": "when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);",
        "line": 258,
        "type": "STUBBING",
        "locate": "@Before",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new AsyncQueryRunner(Executors.newFixedThreadPool(1), new QueryRunner(dataSource));\r\n}"
        }
      },
      {
        "code": "when(conn.createStatement()).thenReturn(stmt);",
        "line": 262,
        "type": "STUBBING",
        "locate": "@Before",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new AsyncQueryRunner(Executors.newFixedThreadPool(1), new QueryRunner(dataSource));\r\n}"
        }
      },
      {
        "code": "callGoodBatch(conn, params);",
        "line": 319,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodBatchDefaultConstructor",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodBatchDefaultConstructor() throws Exception {\r\n    runner = new AsyncQueryRunner(Executors.newFixedThreadPool(1));\r\n    final String[][] params = { { \"unit\", \"unit\" }, { \"test\", \"test\" } };\r\n    callGoodBatch(conn, params);\r\n}"
        }
      },
      {
        "code": "callGoodQuery(conn);",
        "line": 339,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodQueryDefaultConstructor",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodQueryDefaultConstructor() throws Exception {\r\n    runner = new AsyncQueryRunner(Executors.newFixedThreadPool(1));\r\n    callGoodQuery(conn);\r\n}"
        }
      },
      {
        "code": "callGoodQuery(conn);",
        "line": 347,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodQueryPmdTrue",
          "methodAnnotations": [
            "SuppressWarnings",
            "Test"
          ],
          "methodRawCode": "// deliberate test of deprecated code\r\n@SuppressWarnings(\"deprecation\")\r\n@Test\r\npublic void testGoodQueryPmdTrue() throws Exception {\r\n    runner = new AsyncQueryRunner(true, Executors.newFixedThreadPool(1));\r\n    callGoodQuery(conn);\r\n}"
        }
      },
      {
        "code": "callGoodUpdate(conn);",
        "line": 358,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodUpdateDefaultConstructor",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodUpdateDefaultConstructor() throws Exception {\r\n    runner = new AsyncQueryRunner(Executors.newFixedThreadPool(1));\r\n    callGoodUpdate(conn);\r\n}"
        }
      },
      {
        "code": "callGoodUpdate(conn);",
        "line": 365,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodUpdatePmdTrue",
          "methodAnnotations": [
            "SuppressWarnings",
            "Test"
          ],
          "methodRawCode": "// deliberate test of deprecated code\r\n@SuppressWarnings(\"deprecation\")\r\n@Test\r\npublic void testGoodUpdatePmdTrue() throws Exception {\r\n    runner = new AsyncQueryRunner(true, Executors.newFixedThreadPool(1));\r\n    callGoodUpdate(conn);\r\n}"
        }
      },
      {
        "code": "runner.insert(conn, \"3\", handler);",
        "line": 377,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testInsertUsesGivenQueryRunner",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testInsertUsesGivenQueryRunner() throws Exception {\r\n    final QueryRunner mockQueryRunner = mock(QueryRunner.class, // debug for Continuum\r\n    org.mockito.Mockito.withSettings().verboseLogging());\r\n    runner = new AsyncQueryRunner(Executors.newSingleThreadExecutor(), mockQueryRunner);\r\n    runner.insert(\"1\", handler);\r\n    runner.insert(\"2\", handler, \"param1\");\r\n    runner.insert(conn, \"3\", handler);\r\n    runner.insert(conn, \"4\", handler, \"param1\");\r\n    // give the Executor time to submit all insert statements. Otherwise the following verify statements will fail from time to time.\r\n    TimeUnit.MILLISECONDS.sleep(50);\r\n    verify(mockQueryRunner).insert(\"1\", handler);\r\n    verify(mockQueryRunner).insert(\"2\", handler, \"param1\");\r\n    verify(mockQueryRunner).insert(conn, \"3\", handler);\r\n    verify(mockQueryRunner).insert(conn, \"4\", handler, \"param1\");\r\n}"
        }
      },
      {
        "code": "runner.insert(conn, \"4\", handler, \"param1\");",
        "line": 378,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testInsertUsesGivenQueryRunner",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testInsertUsesGivenQueryRunner() throws Exception {\r\n    final QueryRunner mockQueryRunner = mock(QueryRunner.class, // debug for Continuum\r\n    org.mockito.Mockito.withSettings().verboseLogging());\r\n    runner = new AsyncQueryRunner(Executors.newSingleThreadExecutor(), mockQueryRunner);\r\n    runner.insert(\"1\", handler);\r\n    runner.insert(\"2\", handler, \"param1\");\r\n    runner.insert(conn, \"3\", handler);\r\n    runner.insert(conn, \"4\", handler, \"param1\");\r\n    // give the Executor time to submit all insert statements. Otherwise the following verify statements will fail from time to time.\r\n    TimeUnit.MILLISECONDS.sleep(50);\r\n    verify(mockQueryRunner).insert(\"1\", handler);\r\n    verify(mockQueryRunner).insert(\"2\", handler, \"param1\");\r\n    verify(mockQueryRunner).insert(conn, \"3\", handler);\r\n    verify(mockQueryRunner).insert(conn, \"4\", handler, \"param1\");\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 4,
    "variableName": "prepStmt",
    "variableType": "PreparedStatement",
    "isGlobal": true,
    "mockPatternLevel": 2,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\AsyncQueryRunnerTest.java",
      "className": "AsyncQueryRunnerTest"
    },
    "statements": [
      {
        "code": "@Mock\r\nPreparedStatement prepStmt;",
        "line": 55,
        "type": "CREATION",
        "locate": "Attribute",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "FieldDeclaration",
          "methodAnnotations": [
            "Mock"
          ]
        }
      },
      {
        "code": "verify(prepStmt, times(2)).addBatch();",
        "line": 92,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodBatch",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Batch test cases\r\n//\r\nprivate void callGoodBatch(final Connection conn, final Object[][] params) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    final Future<int[]> future = runner.batch(conn, \"select * from blah where ? = ?\", params);\r\n    future.get();\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(prepStmt, times(1)).executeBatch();",
        "line": 93,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodBatch",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Batch test cases\r\n//\r\nprivate void callGoodBatch(final Connection conn, final Object[][] params) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    final Future<int[]> future = runner.batch(conn, \"select * from blah where ? = ?\", params);\r\n    future.get();\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(prepStmt, times(1)).close();",
        "line": 94,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodBatch",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Batch test cases\r\n//\r\nprivate void callGoodBatch(final Connection conn, final Object[][] params) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    final Future<int[]> future = runner.batch(conn, \"select * from blah where ? = ?\", params);\r\n    future.get();\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(prepStmt, times(2)).addBatch();",
        "line": 104,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodBatch",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodBatch(final Object[][] params) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    final Future<int[]> future = runner.batch(\"select * from blah where ? = ?\", params);\r\n    future.get();\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n}"
        }
      },
      {
        "code": "verify(prepStmt, times(1)).executeBatch();",
        "line": 105,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodBatch",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodBatch(final Object[][] params) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    final Future<int[]> future = runner.batch(\"select * from blah where ? = ?\", params);\r\n    future.get();\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(prepStmt, times(1)).close();",
        "line": 106,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodBatch",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodBatch(final Object[][] params) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    final Future<int[]> future = runner.batch(\"select * from blah where ? = ?\", params);\r\n    future.get();\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n}"
        }
      },
      {
        "code": "verify(prepStmt, times(1)).executeQuery();",
        "line": 115,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodQuery() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(sql, handler, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(sql, handler).get();\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(prepStmt, times(1)).close();",
        "line": 117,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodQuery() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(sql, handler, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(sql, handler).get();\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n}"
        }
      },
      {
        "code": "verify(prepStmt, times(1)).executeQuery();",
        "line": 138,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Query test cases\r\n//\r\nprivate void callGoodQuery(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(conn, sql, handler, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(conn, sql, handler).get();\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(prepStmt, times(1)).close();",
        "line": 140,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Query test cases\r\n//\r\nprivate void callGoodQuery(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(conn, sql, handler, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(conn, sql, handler).get();\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(prepStmt, times(1)).executeUpdate();",
        "line": 158,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(\"update blah set unit = ?\", \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(prepStmt, times(1)).close();",
        "line": 159,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(\"update blah set unit = ?\", \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "verify(prepStmt, times(2)).executeUpdate();",
        "line": 175,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(\"update blah set unit = ?\", \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(prepStmt, times(2)).close();",
        "line": 176,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(\"update blah set unit = ?\", \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "verify(prepStmt, times(1)).executeUpdate();",
        "line": 188,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(conn, sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(conn, sql, \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(prepStmt, times(1)).close();",
        "line": 189,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(conn, sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(conn, sql, \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(prepStmt, times(2)).executeUpdate();",
        "line": 206,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(conn, sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(conn, sql, \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(prepStmt, times(2)).close();",
        "line": 207,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(conn, sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(conn, sql, \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);",
        "line": 258,
        "type": "REFERENCE",
        "locate": "@Before",
        "isMockRelated": false,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new AsyncQueryRunner(Executors.newFixedThreadPool(1), new QueryRunner(dataSource));\r\n}"
        }
      },
      {
        "code": "when(prepStmt.getParameterMetaData()).thenReturn(meta);",
        "line": 259,
        "type": "STUBBING",
        "locate": "@Before",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new AsyncQueryRunner(Executors.newFixedThreadPool(1), new QueryRunner(dataSource));\r\n}"
        }
      },
      {
        "code": "when(prepStmt.executeQuery()).thenReturn(results);",
        "line": 260,
        "type": "STUBBING",
        "locate": "@Before",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new AsyncQueryRunner(Executors.newFixedThreadPool(1), new QueryRunner(dataSource));\r\n}"
        }
      },
      {
        "code": "doThrow(new SQLException()).when(prepStmt).executeUpdate();",
        "line": 301,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testExecuteUpdateException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testExecuteUpdateException() throws Exception {\r\n    doThrow(new SQLException()).when(prepStmt).executeUpdate();\r\n    callUpdateWithException(\"unit\", \"test\");\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 5,
    "variableName": "stmt",
    "variableType": "Statement",
    "isGlobal": true,
    "mockPatternLevel": 1,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\AsyncQueryRunnerTest.java",
      "className": "AsyncQueryRunnerTest"
    },
    "statements": [
      {
        "code": "@Mock\r\nStatement stmt;",
        "line": 56,
        "type": "CREATION",
        "locate": "Attribute",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "FieldDeclaration",
          "methodAnnotations": [
            "Mock"
          ]
        }
      },
      {
        "code": "verify(stmt, times(1)).executeQuery(sql);",
        "line": 124,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodQuery() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(sql, handler, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(sql, handler).get();\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(stmt, times(1)).close();",
        "line": 126,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodQuery() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(sql, handler, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(sql, handler).get();\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n}"
        }
      },
      {
        "code": "verify(stmt, times(1)).executeQuery(sql);",
        "line": 147,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Query test cases\r\n//\r\nprivate void callGoodQuery(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(conn, sql, handler, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(conn, sql, handler).get();\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(stmt, times(1)).close();",
        "line": 149,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Query test cases\r\n//\r\nprivate void callGoodQuery(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(conn, sql, handler, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(conn, sql, handler).get();\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(stmt, times(1)).executeUpdate(sql);",
        "line": 167,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(\"update blah set unit = ?\", \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(stmt, times(1)).close();",
        "line": 168,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(\"update blah set unit = ?\", \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "verify(stmt, times(1)).executeUpdate(sql);",
        "line": 197,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(conn, sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(conn, sql, \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(stmt, times(1)).close();",
        "line": 198,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(conn, sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(conn, sql, \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(conn.createStatement()).thenReturn(stmt);",
        "line": 262,
        "type": "REFERENCE",
        "locate": "@Before",
        "isMockRelated": false,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new AsyncQueryRunner(Executors.newFixedThreadPool(1), new QueryRunner(dataSource));\r\n}"
        }
      },
      {
        "code": "when(stmt.executeQuery(any(String.class))).thenReturn(results);",
        "line": 263,
        "type": "STUBBING",
        "locate": "@Before",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new AsyncQueryRunner(Executors.newFixedThreadPool(1), new QueryRunner(dataSource));\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 6,
    "variableName": "meta",
    "variableType": "ParameterMetaData",
    "isGlobal": true,
    "mockPatternLevel": 1,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\AsyncQueryRunnerTest.java",
      "className": "AsyncQueryRunnerTest"
    },
    "statements": [
      {
        "code": "@Mock\r\nParameterMetaData meta;",
        "line": 57,
        "type": "CREATION",
        "locate": "Attribute",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "FieldDeclaration",
          "methodAnnotations": [
            "Mock"
          ]
        }
      },
      {
        "code": "when(meta.getParameterCount()).thenReturn(2);",
        "line": 87,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodBatch",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Batch test cases\r\n//\r\nprivate void callGoodBatch(final Connection conn, final Object[][] params) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    final Future<int[]> future = runner.batch(conn, \"select * from blah where ? = ?\", params);\r\n    future.get();\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(meta.getParameterCount()).thenReturn(2);",
        "line": 99,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodBatch",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodBatch(final Object[][] params) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    final Future<int[]> future = runner.batch(\"select * from blah where ? = ?\", params);\r\n    future.get();\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n}"
        }
      },
      {
        "code": "when(meta.getParameterCount()).thenReturn(2);",
        "line": 111,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodQuery() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(sql, handler, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(sql, handler).get();\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n}"
        }
      },
      {
        "code": "when(meta.getParameterCount()).thenReturn(2);",
        "line": 134,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Query test cases\r\n//\r\nprivate void callGoodQuery(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(conn, sql, handler, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(conn, sql, handler).get();\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(meta.getParameterCount()).thenReturn(2);",
        "line": 154,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(\"update blah set unit = ?\", \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "// call the other variation\r\nwhen(meta.getParameterCount()).thenReturn(0);",
        "line": 163,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(\"update blah set unit = ?\", \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "// call the other variation\r\nwhen(meta.getParameterCount()).thenReturn(1);",
        "line": 172,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(\"update blah set unit = ?\", \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "when(meta.getParameterCount()).thenReturn(2);",
        "line": 184,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(conn, sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(conn, sql, \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// call the other variation\r\nwhen(meta.getParameterCount()).thenReturn(0);",
        "line": 193,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(conn, sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(conn, sql, \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// call the other variation\r\nwhen(meta.getParameterCount()).thenReturn(1);",
        "line": 202,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(conn, sql, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql).get();\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(conn, sql, \"test\").get();\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(prepStmt.getParameterMetaData()).thenReturn(meta);",
        "line": 259,
        "type": "REFERENCE",
        "locate": "@Before",
        "isMockRelated": false,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new AsyncQueryRunner(Executors.newFixedThreadPool(1), new QueryRunner(dataSource));\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 7,
    "variableName": "results",
    "variableType": "ResultSet",
    "isGlobal": true,
    "mockPatternLevel": 1,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\AsyncQueryRunnerTest.java",
      "className": "AsyncQueryRunnerTest"
    },
    "statements": [
      {
        "code": "@Mock\r\nResultSet results;",
        "line": 58,
        "type": "CREATION",
        "locate": "Attribute",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "FieldDeclaration",
          "methodAnnotations": [
            "Mock"
          ]
        }
      },
      {
        "code": "verify(results, times(1)).close();",
        "line": 116,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodQuery() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(sql, handler, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(sql, handler).get();\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n}"
        }
      },
      {
        "code": "verify(results, times(2)).close();",
        "line": 125,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodQuery() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(sql, handler, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(sql, handler).get();\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n}"
        }
      },
      {
        "code": "verify(results, times(1)).close();",
        "line": 139,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Query test cases\r\n//\r\nprivate void callGoodQuery(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(conn, sql, handler, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(conn, sql, handler).get();\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(results, times(2)).close();",
        "line": 148,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Query test cases\r\n//\r\nprivate void callGoodQuery(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(conn, sql, handler, \"unit\", \"test\").get();\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(conn, sql, handler).get();\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(prepStmt.executeQuery()).thenReturn(results);",
        "line": 260,
        "type": "REFERENCE",
        "locate": "@Before",
        "isMockRelated": false,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new AsyncQueryRunner(Executors.newFixedThreadPool(1), new QueryRunner(dataSource));\r\n}"
        }
      },
      {
        "code": "when(stmt.executeQuery(any(String.class))).thenReturn(results);",
        "line": 263,
        "type": "REFERENCE",
        "locate": "@Before",
        "isMockRelated": false,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new AsyncQueryRunner(Executors.newFixedThreadPool(1), new QueryRunner(dataSource));\r\n}"
        }
      },
      {
        "code": "when(results.next()).thenReturn(false);",
        "line": 265,
        "type": "STUBBING",
        "locate": "@Before",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new AsyncQueryRunner(Executors.newFixedThreadPool(1), new QueryRunner(dataSource));\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 8,
    "variableName": "mockCon",
    "variableType": "Connection",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Connection mockCon = mock(Connection.class);",
        "line": 77,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeConnection",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeConnection() throws Exception {\r\n    final Connection mockCon = mock(Connection.class);\r\n    DbUtils.close(mockCon);\r\n    verify(mockCon).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.close(mockCon);",
        "line": 78,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeConnection",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeConnection() throws Exception {\r\n    final Connection mockCon = mock(Connection.class);\r\n    DbUtils.close(mockCon);\r\n    verify(mockCon).close();\r\n}"
        }
      },
      {
        "code": "verify(mockCon).close();",
        "line": 79,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeConnection",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeConnection() throws Exception {\r\n    final Connection mockCon = mock(Connection.class);\r\n    DbUtils.close(mockCon);\r\n    verify(mockCon).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 9,
    "variableName": "mockConnection",
    "variableType": "Connection",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Connection mockConnection = mock(Connection.class);",
        "line": 99,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnection",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnection() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.closeQuietly(mockConnection);\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.closeQuietly(mockConnection);",
        "line": 100,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnection",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnection() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.closeQuietly(mockConnection);\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "verify(mockConnection).close();",
        "line": 101,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnection",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnection() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.closeQuietly(mockConnection);\r\n    verify(mockConnection).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 10,
    "variableName": "mockConnection",
    "variableType": "Connection",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Connection mockConnection = mock(Connection.class);",
        "line": 106,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);",
        "line": 109,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "verify(mockConnection).close();",
        "line": 110,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 11,
    "variableName": "mockResultSet",
    "variableType": "ResultSet",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final ResultSet mockResultSet = mock(ResultSet.class);",
        "line": 107,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);",
        "line": 109,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "verify(mockResultSet).close();",
        "line": 111,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 12,
    "variableName": "mockStatement",
    "variableType": "Statement",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Statement mockStatement = mock(Statement.class);",
        "line": 108,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);",
        "line": 109,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "verify(mockStatement).close();",
        "line": 112,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 13,
    "variableName": "mockConnection",
    "variableType": "Connection",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Connection mockConnection = mock(Connection.class);",
        "line": 117,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetStatementThrowingException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetStatementThrowingException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    doThrow(SQLException.class).when(mockStatement).close();\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);",
        "line": 121,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetStatementThrowingException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetStatementThrowingException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    doThrow(SQLException.class).when(mockStatement).close();\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "verify(mockConnection).close();",
        "line": 122,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetStatementThrowingException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetStatementThrowingException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    doThrow(SQLException.class).when(mockStatement).close();\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 14,
    "variableName": "mockResultSet",
    "variableType": "ResultSet",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final ResultSet mockResultSet = mock(ResultSet.class);",
        "line": 118,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetStatementThrowingException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetStatementThrowingException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    doThrow(SQLException.class).when(mockStatement).close();\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);",
        "line": 121,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetStatementThrowingException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetStatementThrowingException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    doThrow(SQLException.class).when(mockStatement).close();\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "verify(mockResultSet).close();",
        "line": 123,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetStatementThrowingException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetStatementThrowingException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    doThrow(SQLException.class).when(mockStatement).close();\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 15,
    "variableName": "mockStatement",
    "variableType": "Statement",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Statement mockStatement = mock(Statement.class);",
        "line": 119,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetStatementThrowingException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetStatementThrowingException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    doThrow(SQLException.class).when(mockStatement).close();\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "doThrow(SQLException.class).when(mockStatement).close();",
        "line": 120,
        "type": "STUBBING",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetStatementThrowingException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetStatementThrowingException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    doThrow(SQLException.class).when(mockStatement).close();\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);",
        "line": 121,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetStatementThrowingException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetStatementThrowingException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    doThrow(SQLException.class).when(mockStatement).close();\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "verify(mockStatement).close();",
        "line": 124,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetStatementThrowingException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetStatementThrowingException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    doThrow(SQLException.class).when(mockStatement).close();\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 16,
    "variableName": "mockConnection",
    "variableType": "Connection",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Connection mockConnection = mock(Connection.class);",
        "line": 129,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetThrowingExceptionStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetThrowingExceptionStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    doThrow(SQLException.class).when(mockResultSet).close();\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);",
        "line": 133,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetThrowingExceptionStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetThrowingExceptionStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    doThrow(SQLException.class).when(mockResultSet).close();\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "verify(mockConnection).close();",
        "line": 134,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetThrowingExceptionStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetThrowingExceptionStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    doThrow(SQLException.class).when(mockResultSet).close();\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 17,
    "variableName": "mockResultSet",
    "variableType": "ResultSet",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final ResultSet mockResultSet = mock(ResultSet.class);",
        "line": 130,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetThrowingExceptionStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetThrowingExceptionStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    doThrow(SQLException.class).when(mockResultSet).close();\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "doThrow(SQLException.class).when(mockResultSet).close();",
        "line": 131,
        "type": "STUBBING",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetThrowingExceptionStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetThrowingExceptionStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    doThrow(SQLException.class).when(mockResultSet).close();\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);",
        "line": 133,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetThrowingExceptionStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetThrowingExceptionStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    doThrow(SQLException.class).when(mockResultSet).close();\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "verify(mockResultSet).close();",
        "line": 135,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetThrowingExceptionStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetThrowingExceptionStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    doThrow(SQLException.class).when(mockResultSet).close();\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 18,
    "variableName": "mockStatement",
    "variableType": "Statement",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Statement mockStatement = mock(Statement.class);",
        "line": 132,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetThrowingExceptionStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetThrowingExceptionStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    doThrow(SQLException.class).when(mockResultSet).close();\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);",
        "line": 133,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetThrowingExceptionStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetThrowingExceptionStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    doThrow(SQLException.class).when(mockResultSet).close();\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "verify(mockStatement).close();",
        "line": 136,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionResultSetThrowingExceptionStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionResultSetThrowingExceptionStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    doThrow(SQLException.class).when(mockResultSet).close();\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 19,
    "variableName": "mockConnection",
    "variableType": "Connection",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Connection mockConnection = mock(Connection.class);",
        "line": 141,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionThrowingException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionThrowingException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).close();\r\n    DbUtils.closeQuietly(mockConnection);\r\n}"
        }
      },
      {
        "code": "doThrow(SQLException.class).when(mockConnection).close();",
        "line": 142,
        "type": "STUBBING",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionThrowingException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionThrowingException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).close();\r\n    DbUtils.closeQuietly(mockConnection);\r\n}"
        }
      },
      {
        "code": "DbUtils.closeQuietly(mockConnection);",
        "line": 143,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionThrowingException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionThrowingException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).close();\r\n    DbUtils.closeQuietly(mockConnection);\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 20,
    "variableName": "mockConnection",
    "variableType": "Connection",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Connection mockConnection = mock(Connection.class);",
        "line": 148,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionThrowingExceptionResultSetStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionThrowingExceptionResultSetStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).close();\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "doThrow(SQLException.class).when(mockConnection).close();",
        "line": 149,
        "type": "STUBBING",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionThrowingExceptionResultSetStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionThrowingExceptionResultSetStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).close();\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);",
        "line": 152,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionThrowingExceptionResultSetStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionThrowingExceptionResultSetStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).close();\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "verify(mockConnection).close();",
        "line": 153,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionThrowingExceptionResultSetStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionThrowingExceptionResultSetStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).close();\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 21,
    "variableName": "mockResultSet",
    "variableType": "ResultSet",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final ResultSet mockResultSet = mock(ResultSet.class);",
        "line": 150,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionThrowingExceptionResultSetStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionThrowingExceptionResultSetStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).close();\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);",
        "line": 152,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionThrowingExceptionResultSetStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionThrowingExceptionResultSetStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).close();\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "verify(mockResultSet).close();",
        "line": 154,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionThrowingExceptionResultSetStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionThrowingExceptionResultSetStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).close();\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 22,
    "variableName": "mockStatement",
    "variableType": "Statement",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Statement mockStatement = mock(Statement.class);",
        "line": 151,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionThrowingExceptionResultSetStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionThrowingExceptionResultSetStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).close();\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);",
        "line": 152,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionThrowingExceptionResultSetStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionThrowingExceptionResultSetStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).close();\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "verify(mockStatement).close();",
        "line": 155,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyConnectionThrowingExceptionResultSetStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyConnectionThrowingExceptionResultSetStatement() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).close();\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockConnection, mockStatement, mockResultSet);\r\n    verify(mockConnection).close();\r\n    verify(mockResultSet).close();\r\n    verify(mockStatement).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 23,
    "variableName": "mockResultSet",
    "variableType": "ResultSet",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final ResultSet mockResultSet = mock(ResultSet.class);",
        "line": 175,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyResultSet",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyResultSet() throws Exception {\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    DbUtils.closeQuietly(mockResultSet);\r\n    verify(mockResultSet).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.closeQuietly(mockResultSet);",
        "line": 176,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyResultSet",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyResultSet() throws Exception {\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    DbUtils.closeQuietly(mockResultSet);\r\n    verify(mockResultSet).close();\r\n}"
        }
      },
      {
        "code": "verify(mockResultSet).close();",
        "line": 177,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyResultSet",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyResultSet() throws Exception {\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    DbUtils.closeQuietly(mockResultSet);\r\n    verify(mockResultSet).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 24,
    "variableName": "mockResultSet",
    "variableType": "ResultSet",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final ResultSet mockResultSet = mock(ResultSet.class);",
        "line": 182,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyResultSetThrowingException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyResultSetThrowingException() throws Exception {\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    doThrow(SQLException.class).when(mockResultSet).close();\r\n    DbUtils.closeQuietly(mockResultSet);\r\n}"
        }
      },
      {
        "code": "doThrow(SQLException.class).when(mockResultSet).close();",
        "line": 183,
        "type": "STUBBING",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyResultSetThrowingException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyResultSetThrowingException() throws Exception {\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    doThrow(SQLException.class).when(mockResultSet).close();\r\n    DbUtils.closeQuietly(mockResultSet);\r\n}"
        }
      },
      {
        "code": "DbUtils.closeQuietly(mockResultSet);",
        "line": 184,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyResultSetThrowingException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyResultSetThrowingException() throws Exception {\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    doThrow(SQLException.class).when(mockResultSet).close();\r\n    DbUtils.closeQuietly(mockResultSet);\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 25,
    "variableName": "mockStatement",
    "variableType": "Statement",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Statement mockStatement = mock(Statement.class);",
        "line": 189,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyStatement() throws Exception {\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockStatement);\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.closeQuietly(mockStatement);",
        "line": 190,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyStatement() throws Exception {\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockStatement);\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "verify(mockStatement).close();",
        "line": 191,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyStatement() throws Exception {\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.closeQuietly(mockStatement);\r\n    verify(mockStatement).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 26,
    "variableName": "mockStatement",
    "variableType": "Statement",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Statement mockStatement = mock(Statement.class);",
        "line": 196,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyStatementThrowingException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyStatementThrowingException() throws Exception {\r\n    final Statement mockStatement = mock(Statement.class);\r\n    doThrow(SQLException.class).when(mockStatement).close();\r\n    DbUtils.closeQuietly(mockStatement);\r\n}"
        }
      },
      {
        "code": "doThrow(SQLException.class).when(mockStatement).close();",
        "line": 197,
        "type": "STUBBING",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyStatementThrowingException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyStatementThrowingException() throws Exception {\r\n    final Statement mockStatement = mock(Statement.class);\r\n    doThrow(SQLException.class).when(mockStatement).close();\r\n    DbUtils.closeQuietly(mockStatement);\r\n}"
        }
      },
      {
        "code": "DbUtils.closeQuietly(mockStatement);",
        "line": 198,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeQuietlyStatementThrowingException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeQuietlyStatementThrowingException() throws Exception {\r\n    final Statement mockStatement = mock(Statement.class);\r\n    doThrow(SQLException.class).when(mockStatement).close();\r\n    DbUtils.closeQuietly(mockStatement);\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 27,
    "variableName": "mockResultSet",
    "variableType": "ResultSet",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final ResultSet mockResultSet = mock(ResultSet.class);",
        "line": 203,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeResultSet",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeResultSet() throws Exception {\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    DbUtils.close(mockResultSet);\r\n    verify(mockResultSet).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.close(mockResultSet);",
        "line": 204,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeResultSet",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeResultSet() throws Exception {\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    DbUtils.close(mockResultSet);\r\n    verify(mockResultSet).close();\r\n}"
        }
      },
      {
        "code": "verify(mockResultSet).close();",
        "line": 205,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeResultSet",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeResultSet() throws Exception {\r\n    final ResultSet mockResultSet = mock(ResultSet.class);\r\n    DbUtils.close(mockResultSet);\r\n    verify(mockResultSet).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 28,
    "variableName": "mockStatement",
    "variableType": "Statement",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Statement mockStatement = mock(Statement.class);",
        "line": 210,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeStatement() throws Exception {\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.close(mockStatement);\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.close(mockStatement);",
        "line": 211,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeStatement() throws Exception {\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.close(mockStatement);\r\n    verify(mockStatement).close();\r\n}"
        }
      },
      {
        "code": "verify(mockStatement).close();",
        "line": 212,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "closeStatement",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void closeStatement() throws Exception {\r\n    final Statement mockStatement = mock(Statement.class);\r\n    DbUtils.close(mockStatement);\r\n    verify(mockStatement).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 29,
    "variableName": "mockConnection",
    "variableType": "Connection",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Connection mockConnection = mock(Connection.class);",
        "line": 217,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "commitAndClose",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void commitAndClose() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.commitAndClose(mockConnection);\r\n    verify(mockConnection).commit();\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.commitAndClose(mockConnection);",
        "line": 218,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "commitAndClose",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void commitAndClose() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.commitAndClose(mockConnection);\r\n    verify(mockConnection).commit();\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "verify(mockConnection).commit();",
        "line": 219,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "commitAndClose",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void commitAndClose() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.commitAndClose(mockConnection);\r\n    verify(mockConnection).commit();\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "verify(mockConnection).close();",
        "line": 220,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "commitAndClose",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void commitAndClose() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.commitAndClose(mockConnection);\r\n    verify(mockConnection).commit();\r\n    verify(mockConnection).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 30,
    "variableName": "mockConnection",
    "variableType": "Connection",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Connection mockConnection = mock(Connection.class);",
        "line": 225,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "commitAndCloseQuietly",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void commitAndCloseQuietly() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.commitAndClose(mockConnection);\r\n    verify(mockConnection).commit();\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.commitAndClose(mockConnection);",
        "line": 226,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "commitAndCloseQuietly",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void commitAndCloseQuietly() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.commitAndClose(mockConnection);\r\n    verify(mockConnection).commit();\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "verify(mockConnection).commit();",
        "line": 227,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "commitAndCloseQuietly",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void commitAndCloseQuietly() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.commitAndClose(mockConnection);\r\n    verify(mockConnection).commit();\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "verify(mockConnection).close();",
        "line": 228,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "commitAndCloseQuietly",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void commitAndCloseQuietly() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.commitAndClose(mockConnection);\r\n    verify(mockConnection).commit();\r\n    verify(mockConnection).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 31,
    "variableName": "mockConnection",
    "variableType": "Connection",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Connection mockConnection = mock(Connection.class);",
        "line": 233,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "commitAndCloseQuietlyWithException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void commitAndCloseQuietlyWithException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).close();\r\n    DbUtils.commitAndCloseQuietly(mockConnection);\r\n    verify(mockConnection).commit();\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "doThrow(SQLException.class).when(mockConnection).close();",
        "line": 234,
        "type": "STUBBING",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "commitAndCloseQuietlyWithException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void commitAndCloseQuietlyWithException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).close();\r\n    DbUtils.commitAndCloseQuietly(mockConnection);\r\n    verify(mockConnection).commit();\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.commitAndCloseQuietly(mockConnection);",
        "line": 235,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "commitAndCloseQuietlyWithException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void commitAndCloseQuietlyWithException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).close();\r\n    DbUtils.commitAndCloseQuietly(mockConnection);\r\n    verify(mockConnection).commit();\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "verify(mockConnection).commit();",
        "line": 236,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "commitAndCloseQuietlyWithException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void commitAndCloseQuietlyWithException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).close();\r\n    DbUtils.commitAndCloseQuietly(mockConnection);\r\n    verify(mockConnection).commit();\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "verify(mockConnection).close();",
        "line": 237,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "commitAndCloseQuietlyWithException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void commitAndCloseQuietlyWithException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).close();\r\n    DbUtils.commitAndCloseQuietly(mockConnection);\r\n    verify(mockConnection).commit();\r\n    verify(mockConnection).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 32,
    "variableName": "mockConnection",
    "variableType": "Connection",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Connection mockConnection = mock(Connection.class);",
        "line": 242,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "commitAndCloseWithException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void commitAndCloseWithException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).commit();\r\n    try {\r\n        DbUtils.commitAndClose(mockConnection);\r\n        fail(\"DbUtils.commitAndClose() swallowed SQLEception!\");\r\n    } catch (final SQLException e) {\r\n        // we expect this exception\r\n    }\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "doThrow(SQLException.class).when(mockConnection).commit();",
        "line": 243,
        "type": "STUBBING",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "commitAndCloseWithException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void commitAndCloseWithException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).commit();\r\n    try {\r\n        DbUtils.commitAndClose(mockConnection);\r\n        fail(\"DbUtils.commitAndClose() swallowed SQLEception!\");\r\n    } catch (final SQLException e) {\r\n        // we expect this exception\r\n    }\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "verify(mockConnection).close();",
        "line": 250,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "commitAndCloseWithException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void commitAndCloseWithException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).commit();\r\n    try {\r\n        DbUtils.commitAndClose(mockConnection);\r\n        fail(\"DbUtils.commitAndClose() swallowed SQLEception!\");\r\n    } catch (final SQLException e) {\r\n        // we expect this exception\r\n    }\r\n    verify(mockConnection).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 33,
    "variableName": "mockConnection",
    "variableType": "Connection",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Connection mockConnection = mock(Connection.class);",
        "line": 255,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollback",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollback() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.rollback(mockConnection);\r\n    verify(mockConnection).rollback();\r\n}"
        }
      },
      {
        "code": "DbUtils.rollback(mockConnection);",
        "line": 256,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollback",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollback() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.rollback(mockConnection);\r\n    verify(mockConnection).rollback();\r\n}"
        }
      },
      {
        "code": "verify(mockConnection).rollback();",
        "line": 257,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollback",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollback() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.rollback(mockConnection);\r\n    verify(mockConnection).rollback();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 34,
    "variableName": "mockConnection",
    "variableType": "Connection",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Connection mockConnection = mock(Connection.class);",
        "line": 262,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackAndClose",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackAndClose() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.rollbackAndClose(mockConnection);\r\n    verify(mockConnection).rollback();\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.rollbackAndClose(mockConnection);",
        "line": 263,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackAndClose",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackAndClose() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.rollbackAndClose(mockConnection);\r\n    verify(mockConnection).rollback();\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "verify(mockConnection).rollback();",
        "line": 264,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackAndClose",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackAndClose() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.rollbackAndClose(mockConnection);\r\n    verify(mockConnection).rollback();\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "verify(mockConnection).close();",
        "line": 265,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackAndClose",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackAndClose() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.rollbackAndClose(mockConnection);\r\n    verify(mockConnection).rollback();\r\n    verify(mockConnection).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 35,
    "variableName": "mockConnection",
    "variableType": "Connection",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Connection mockConnection = mock(Connection.class);",
        "line": 275,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackAndCloseQuietly",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackAndCloseQuietly() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.rollbackAndCloseQuietly(mockConnection);\r\n    verify(mockConnection).rollback();\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.rollbackAndCloseQuietly(mockConnection);",
        "line": 276,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackAndCloseQuietly",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackAndCloseQuietly() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.rollbackAndCloseQuietly(mockConnection);\r\n    verify(mockConnection).rollback();\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "verify(mockConnection).rollback();",
        "line": 277,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackAndCloseQuietly",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackAndCloseQuietly() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.rollbackAndCloseQuietly(mockConnection);\r\n    verify(mockConnection).rollback();\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "verify(mockConnection).close();",
        "line": 278,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackAndCloseQuietly",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackAndCloseQuietly() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.rollbackAndCloseQuietly(mockConnection);\r\n    verify(mockConnection).rollback();\r\n    verify(mockConnection).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 36,
    "variableName": "mockConnection",
    "variableType": "Connection",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Connection mockConnection = mock(Connection.class);",
        "line": 288,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackAndCloseQuietlyWithException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackAndCloseQuietlyWithException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).rollback();\r\n    DbUtils.rollbackAndCloseQuietly(mockConnection);\r\n    verify(mockConnection).rollback();\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "doThrow(SQLException.class).when(mockConnection).rollback();",
        "line": 289,
        "type": "STUBBING",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackAndCloseQuietlyWithException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackAndCloseQuietlyWithException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).rollback();\r\n    DbUtils.rollbackAndCloseQuietly(mockConnection);\r\n    verify(mockConnection).rollback();\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "DbUtils.rollbackAndCloseQuietly(mockConnection);",
        "line": 290,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackAndCloseQuietlyWithException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackAndCloseQuietlyWithException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).rollback();\r\n    DbUtils.rollbackAndCloseQuietly(mockConnection);\r\n    verify(mockConnection).rollback();\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "verify(mockConnection).rollback();",
        "line": 291,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackAndCloseQuietlyWithException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackAndCloseQuietlyWithException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).rollback();\r\n    DbUtils.rollbackAndCloseQuietly(mockConnection);\r\n    verify(mockConnection).rollback();\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "verify(mockConnection).close();",
        "line": 292,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackAndCloseQuietlyWithException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackAndCloseQuietlyWithException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).rollback();\r\n    DbUtils.rollbackAndCloseQuietly(mockConnection);\r\n    verify(mockConnection).rollback();\r\n    verify(mockConnection).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 37,
    "variableName": "mockConnection",
    "variableType": "Connection",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Connection mockConnection = mock(Connection.class);",
        "line": 297,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackAndCloseWithException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackAndCloseWithException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).rollback();\r\n    try {\r\n        DbUtils.rollbackAndClose(mockConnection);\r\n        fail(\"DbUtils.rollbackAndClose() swallowed SQLException!\");\r\n    } catch (final SQLException e) {\r\n        // we expect this exception\r\n    }\r\n    verify(mockConnection).rollback();\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "doThrow(SQLException.class).when(mockConnection).rollback();",
        "line": 298,
        "type": "STUBBING",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackAndCloseWithException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackAndCloseWithException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).rollback();\r\n    try {\r\n        DbUtils.rollbackAndClose(mockConnection);\r\n        fail(\"DbUtils.rollbackAndClose() swallowed SQLException!\");\r\n    } catch (final SQLException e) {\r\n        // we expect this exception\r\n    }\r\n    verify(mockConnection).rollback();\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "verify(mockConnection).rollback();",
        "line": 305,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackAndCloseWithException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackAndCloseWithException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).rollback();\r\n    try {\r\n        DbUtils.rollbackAndClose(mockConnection);\r\n        fail(\"DbUtils.rollbackAndClose() swallowed SQLException!\");\r\n    } catch (final SQLException e) {\r\n        // we expect this exception\r\n    }\r\n    verify(mockConnection).rollback();\r\n    verify(mockConnection).close();\r\n}"
        }
      },
      {
        "code": "verify(mockConnection).close();",
        "line": 306,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackAndCloseWithException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackAndCloseWithException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).rollback();\r\n    try {\r\n        DbUtils.rollbackAndClose(mockConnection);\r\n        fail(\"DbUtils.rollbackAndClose() swallowed SQLException!\");\r\n    } catch (final SQLException e) {\r\n        // we expect this exception\r\n    }\r\n    verify(mockConnection).rollback();\r\n    verify(mockConnection).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 38,
    "variableName": "mockConnection",
    "variableType": "Connection",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Connection mockConnection = mock(Connection.class);",
        "line": 316,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackQuietly",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackQuietly() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.rollbackQuietly(mockConnection);\r\n    verify(mockConnection).rollback();\r\n}"
        }
      },
      {
        "code": "DbUtils.rollbackQuietly(mockConnection);",
        "line": 317,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackQuietly",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackQuietly() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.rollbackQuietly(mockConnection);\r\n    verify(mockConnection).rollback();\r\n}"
        }
      },
      {
        "code": "verify(mockConnection).rollback();",
        "line": 318,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackQuietly",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackQuietly() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    DbUtils.rollbackQuietly(mockConnection);\r\n    verify(mockConnection).rollback();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 39,
    "variableName": "mockConnection",
    "variableType": "Connection",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java"
    },
    "statements": [
      {
        "code": "final Connection mockConnection = mock(Connection.class);",
        "line": 328,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackQuietlyWithException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackQuietlyWithException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).rollback();\r\n    DbUtils.rollbackQuietly(mockConnection);\r\n    verify(mockConnection).rollback();\r\n}"
        }
      },
      {
        "code": "doThrow(SQLException.class).when(mockConnection).rollback();",
        "line": 329,
        "type": "STUBBING",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackQuietlyWithException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackQuietlyWithException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).rollback();\r\n    DbUtils.rollbackQuietly(mockConnection);\r\n    verify(mockConnection).rollback();\r\n}"
        }
      },
      {
        "code": "DbUtils.rollbackQuietly(mockConnection);",
        "line": 330,
        "type": "REFERENCE",
        "locate": "",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackQuietlyWithException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackQuietlyWithException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).rollback();\r\n    DbUtils.rollbackQuietly(mockConnection);\r\n    verify(mockConnection).rollback();\r\n}"
        }
      },
      {
        "code": "verify(mockConnection).rollback();",
        "line": 331,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "rollbackQuietlyWithException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void rollbackQuietlyWithException() throws Exception {\r\n    final Connection mockConnection = mock(Connection.class);\r\n    doThrow(SQLException.class).when(mockConnection).rollback();\r\n    DbUtils.rollbackQuietly(mockConnection);\r\n    verify(mockConnection).rollback();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 40,
    "variableName": "mockedDriver",
    "variableType": "Driver",
    "isGlobal": true,
    "mockPatternLevel": 1,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\DbUtilsTest.java",
      "className": "DriverProxyTest"
    },
    "statements": [
      {
        "code": "private static final Driver mockedDriver = mock(Driver.class);",
        "line": 40,
        "type": "CREATION",
        "locate": "Attribute",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "FieldDeclaration",
          "methodAnnotations": []
        }
      },
      {
        "code": "reset(mockedDriver);",
        "line": 50,
        "type": "REFERENCE",
        "locate": "@After",
        "isMockRelated": false,
        "isShareable": true,
        "locationContext": {
          "methodName": "tearDown",
          "methodAnnotations": [
            "After"
          ],
          "methodRawCode": "@After\r\npublic void tearDown() {\r\n    reset(mockedDriver);\r\n}"
        }
      },
      {
        "code": "verify(mockedDriver).getMajorVersion();",
        "line": 56,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testProxiedMethods",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testProxiedMethods() throws Exception {\r\n    proxy.getMajorVersion();\r\n    verify(mockedDriver).getMajorVersion();\r\n    proxy.getMinorVersion();\r\n    verify(mockedDriver).getMinorVersion();\r\n    proxy.jdbcCompliant();\r\n    verify(mockedDriver).jdbcCompliant();\r\n    final String url = \"testUrl\";\r\n    proxy.acceptsURL(url);\r\n    verify(mockedDriver).acceptsURL(url);\r\n    final Properties props = new Properties();\r\n    props.setProperty(\"test\", \"true\");\r\n    proxy.connect(url, props);\r\n    verify(mockedDriver).connect(url, props);\r\n    proxy.getPropertyInfo(url, props);\r\n    verify(mockedDriver).getPropertyInfo(url, props);\r\n}"
        }
      },
      {
        "code": "verify(mockedDriver).getMinorVersion();",
        "line": 58,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testProxiedMethods",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testProxiedMethods() throws Exception {\r\n    proxy.getMajorVersion();\r\n    verify(mockedDriver).getMajorVersion();\r\n    proxy.getMinorVersion();\r\n    verify(mockedDriver).getMinorVersion();\r\n    proxy.jdbcCompliant();\r\n    verify(mockedDriver).jdbcCompliant();\r\n    final String url = \"testUrl\";\r\n    proxy.acceptsURL(url);\r\n    verify(mockedDriver).acceptsURL(url);\r\n    final Properties props = new Properties();\r\n    props.setProperty(\"test\", \"true\");\r\n    proxy.connect(url, props);\r\n    verify(mockedDriver).connect(url, props);\r\n    proxy.getPropertyInfo(url, props);\r\n    verify(mockedDriver).getPropertyInfo(url, props);\r\n}"
        }
      },
      {
        "code": "verify(mockedDriver).jdbcCompliant();",
        "line": 60,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testProxiedMethods",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testProxiedMethods() throws Exception {\r\n    proxy.getMajorVersion();\r\n    verify(mockedDriver).getMajorVersion();\r\n    proxy.getMinorVersion();\r\n    verify(mockedDriver).getMinorVersion();\r\n    proxy.jdbcCompliant();\r\n    verify(mockedDriver).jdbcCompliant();\r\n    final String url = \"testUrl\";\r\n    proxy.acceptsURL(url);\r\n    verify(mockedDriver).acceptsURL(url);\r\n    final Properties props = new Properties();\r\n    props.setProperty(\"test\", \"true\");\r\n    proxy.connect(url, props);\r\n    verify(mockedDriver).connect(url, props);\r\n    proxy.getPropertyInfo(url, props);\r\n    verify(mockedDriver).getPropertyInfo(url, props);\r\n}"
        }
      },
      {
        "code": "verify(mockedDriver).acceptsURL(url);",
        "line": 64,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testProxiedMethods",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testProxiedMethods() throws Exception {\r\n    proxy.getMajorVersion();\r\n    verify(mockedDriver).getMajorVersion();\r\n    proxy.getMinorVersion();\r\n    verify(mockedDriver).getMinorVersion();\r\n    proxy.jdbcCompliant();\r\n    verify(mockedDriver).jdbcCompliant();\r\n    final String url = \"testUrl\";\r\n    proxy.acceptsURL(url);\r\n    verify(mockedDriver).acceptsURL(url);\r\n    final Properties props = new Properties();\r\n    props.setProperty(\"test\", \"true\");\r\n    proxy.connect(url, props);\r\n    verify(mockedDriver).connect(url, props);\r\n    proxy.getPropertyInfo(url, props);\r\n    verify(mockedDriver).getPropertyInfo(url, props);\r\n}"
        }
      },
      {
        "code": "verify(mockedDriver).connect(url, props);",
        "line": 69,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testProxiedMethods",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testProxiedMethods() throws Exception {\r\n    proxy.getMajorVersion();\r\n    verify(mockedDriver).getMajorVersion();\r\n    proxy.getMinorVersion();\r\n    verify(mockedDriver).getMinorVersion();\r\n    proxy.jdbcCompliant();\r\n    verify(mockedDriver).jdbcCompliant();\r\n    final String url = \"testUrl\";\r\n    proxy.acceptsURL(url);\r\n    verify(mockedDriver).acceptsURL(url);\r\n    final Properties props = new Properties();\r\n    props.setProperty(\"test\", \"true\");\r\n    proxy.connect(url, props);\r\n    verify(mockedDriver).connect(url, props);\r\n    proxy.getPropertyInfo(url, props);\r\n    verify(mockedDriver).getPropertyInfo(url, props);\r\n}"
        }
      },
      {
        "code": "verify(mockedDriver).getPropertyInfo(url, props);",
        "line": 71,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testProxiedMethods",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testProxiedMethods() throws Exception {\r\n    proxy.getMajorVersion();\r\n    verify(mockedDriver).getMajorVersion();\r\n    proxy.getMinorVersion();\r\n    verify(mockedDriver).getMinorVersion();\r\n    proxy.jdbcCompliant();\r\n    verify(mockedDriver).jdbcCompliant();\r\n    final String url = \"testUrl\";\r\n    proxy.acceptsURL(url);\r\n    verify(mockedDriver).acceptsURL(url);\r\n    final Properties props = new Properties();\r\n    props.setProperty(\"test\", \"true\");\r\n    proxy.connect(url, props);\r\n    verify(mockedDriver).connect(url, props);\r\n    proxy.getPropertyInfo(url, props);\r\n    verify(mockedDriver).getPropertyInfo(url, props);\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 41,
    "variableName": "metaData",
    "variableType": "ResultSetMetaData",
    "isGlobal": true,
    "mockPatternLevel": 2,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\GenerousBeanProcessorTest.java",
      "className": "GenerousBeanProcessorTest"
    },
    "statements": [
      {
        "code": "@Mock\r\nResultSetMetaData metaData;",
        "line": 65,
        "type": "CREATION",
        "locate": "Attribute",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "FieldDeclaration",
          "methodAnnotations": [
            "Mock"
          ]
        }
      },
      {
        "code": "when(metaData.getColumnCount()).thenReturn(1);",
        "line": 81,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testMapColumnsToPropertiesColumnLabelIsNull",
          "methodAnnotations": [
            "SuppressWarnings",
            "Test"
          ],
          "methodRawCode": "// test code\r\n@SuppressWarnings(\"boxing\")\r\n@Test\r\npublic void testMapColumnsToPropertiesColumnLabelIsNull() throws Exception {\r\n    when(metaData.getColumnCount()).thenReturn(1);\r\n    when(metaData.getColumnName(1)).thenReturn(\"juhu\");\r\n    when(metaData.getColumnLabel(1)).thenReturn(null);\r\n    final int[] ret = processor.mapColumnsToProperties(metaData, propDescriptors);\r\n    assertNotNull(ret);\r\n    assertEquals(2, ret.length);\r\n    assertEquals(-1, ret[0]);\r\n    assertEquals(-1, ret[1]);\r\n}"
        }
      },
      {
        "code": "when(metaData.getColumnName(1)).thenReturn(\"juhu\");",
        "line": 82,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testMapColumnsToPropertiesColumnLabelIsNull",
          "methodAnnotations": [
            "SuppressWarnings",
            "Test"
          ],
          "methodRawCode": "// test code\r\n@SuppressWarnings(\"boxing\")\r\n@Test\r\npublic void testMapColumnsToPropertiesColumnLabelIsNull() throws Exception {\r\n    when(metaData.getColumnCount()).thenReturn(1);\r\n    when(metaData.getColumnName(1)).thenReturn(\"juhu\");\r\n    when(metaData.getColumnLabel(1)).thenReturn(null);\r\n    final int[] ret = processor.mapColumnsToProperties(metaData, propDescriptors);\r\n    assertNotNull(ret);\r\n    assertEquals(2, ret.length);\r\n    assertEquals(-1, ret[0]);\r\n    assertEquals(-1, ret[1]);\r\n}"
        }
      },
      {
        "code": "when(metaData.getColumnLabel(1)).thenReturn(null);",
        "line": 83,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testMapColumnsToPropertiesColumnLabelIsNull",
          "methodAnnotations": [
            "SuppressWarnings",
            "Test"
          ],
          "methodRawCode": "// test code\r\n@SuppressWarnings(\"boxing\")\r\n@Test\r\npublic void testMapColumnsToPropertiesColumnLabelIsNull() throws Exception {\r\n    when(metaData.getColumnCount()).thenReturn(1);\r\n    when(metaData.getColumnName(1)).thenReturn(\"juhu\");\r\n    when(metaData.getColumnLabel(1)).thenReturn(null);\r\n    final int[] ret = processor.mapColumnsToProperties(metaData, propDescriptors);\r\n    assertNotNull(ret);\r\n    assertEquals(2, ret.length);\r\n    assertEquals(-1, ret[0]);\r\n    assertEquals(-1, ret[1]);\r\n}"
        }
      },
      {
        "code": "when(metaData.getColumnCount()).thenReturn(3);",
        "line": 96,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testMapColumnsToPropertiesMixedCase",
          "methodAnnotations": [
            "SuppressWarnings",
            "Test"
          ],
          "methodRawCode": "// test code\r\n@SuppressWarnings(\"boxing\")\r\n@Test\r\npublic void testMapColumnsToPropertiesMixedCase() throws Exception {\r\n    when(metaData.getColumnCount()).thenReturn(3);\r\n    when(metaData.getColumnLabel(1)).thenReturn(\"tHree\");\r\n    when(metaData.getColumnLabel(2)).thenReturn(\"One\");\r\n    when(metaData.getColumnLabel(3)).thenReturn(\"tWO\");\r\n    final int[] ret = processor.mapColumnsToProperties(metaData, propDescriptors);\r\n    assertNotNull(ret);\r\n    assertEquals(4, ret.length);\r\n    assertEquals(-1, ret[0]);\r\n    assertEquals(2, ret[1]);\r\n    assertEquals(0, ret[2]);\r\n    assertEquals(1, ret[3]);\r\n}"
        }
      },
      {
        "code": "when(metaData.getColumnLabel(1)).thenReturn(\"tHree\");",
        "line": 98,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testMapColumnsToPropertiesMixedCase",
          "methodAnnotations": [
            "SuppressWarnings",
            "Test"
          ],
          "methodRawCode": "// test code\r\n@SuppressWarnings(\"boxing\")\r\n@Test\r\npublic void testMapColumnsToPropertiesMixedCase() throws Exception {\r\n    when(metaData.getColumnCount()).thenReturn(3);\r\n    when(metaData.getColumnLabel(1)).thenReturn(\"tHree\");\r\n    when(metaData.getColumnLabel(2)).thenReturn(\"One\");\r\n    when(metaData.getColumnLabel(3)).thenReturn(\"tWO\");\r\n    final int[] ret = processor.mapColumnsToProperties(metaData, propDescriptors);\r\n    assertNotNull(ret);\r\n    assertEquals(4, ret.length);\r\n    assertEquals(-1, ret[0]);\r\n    assertEquals(2, ret[1]);\r\n    assertEquals(0, ret[2]);\r\n    assertEquals(1, ret[3]);\r\n}"
        }
      },
      {
        "code": "when(metaData.getColumnLabel(2)).thenReturn(\"One\");",
        "line": 99,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testMapColumnsToPropertiesMixedCase",
          "methodAnnotations": [
            "SuppressWarnings",
            "Test"
          ],
          "methodRawCode": "// test code\r\n@SuppressWarnings(\"boxing\")\r\n@Test\r\npublic void testMapColumnsToPropertiesMixedCase() throws Exception {\r\n    when(metaData.getColumnCount()).thenReturn(3);\r\n    when(metaData.getColumnLabel(1)).thenReturn(\"tHree\");\r\n    when(metaData.getColumnLabel(2)).thenReturn(\"One\");\r\n    when(metaData.getColumnLabel(3)).thenReturn(\"tWO\");\r\n    final int[] ret = processor.mapColumnsToProperties(metaData, propDescriptors);\r\n    assertNotNull(ret);\r\n    assertEquals(4, ret.length);\r\n    assertEquals(-1, ret[0]);\r\n    assertEquals(2, ret[1]);\r\n    assertEquals(0, ret[2]);\r\n    assertEquals(1, ret[3]);\r\n}"
        }
      },
      {
        "code": "when(metaData.getColumnLabel(3)).thenReturn(\"tWO\");",
        "line": 100,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testMapColumnsToPropertiesMixedCase",
          "methodAnnotations": [
            "SuppressWarnings",
            "Test"
          ],
          "methodRawCode": "// test code\r\n@SuppressWarnings(\"boxing\")\r\n@Test\r\npublic void testMapColumnsToPropertiesMixedCase() throws Exception {\r\n    when(metaData.getColumnCount()).thenReturn(3);\r\n    when(metaData.getColumnLabel(1)).thenReturn(\"tHree\");\r\n    when(metaData.getColumnLabel(2)).thenReturn(\"One\");\r\n    when(metaData.getColumnLabel(3)).thenReturn(\"tWO\");\r\n    final int[] ret = processor.mapColumnsToProperties(metaData, propDescriptors);\r\n    assertNotNull(ret);\r\n    assertEquals(4, ret.length);\r\n    assertEquals(-1, ret[0]);\r\n    assertEquals(2, ret[1]);\r\n    assertEquals(0, ret[2]);\r\n    assertEquals(1, ret[3]);\r\n}"
        }
      },
      {
        "code": "when(metaData.getColumnCount()).thenReturn(3);",
        "line": 115,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testMapColumnsToPropertiesWithOutUnderscores",
          "methodAnnotations": [
            "SuppressWarnings",
            "Test"
          ],
          "methodRawCode": "// test code\r\n@SuppressWarnings(\"boxing\")\r\n@Test\r\npublic void testMapColumnsToPropertiesWithOutUnderscores() throws Exception {\r\n    when(metaData.getColumnCount()).thenReturn(3);\r\n    when(metaData.getColumnLabel(1)).thenReturn(\"three\");\r\n    when(metaData.getColumnLabel(2)).thenReturn(\"one\");\r\n    when(metaData.getColumnLabel(3)).thenReturn(\"two\");\r\n    final int[] ret = processor.mapColumnsToProperties(metaData, propDescriptors);\r\n    assertNotNull(ret);\r\n    assertEquals(4, ret.length);\r\n    assertEquals(-1, ret[0]);\r\n    assertEquals(2, ret[1]);\r\n    assertEquals(0, ret[2]);\r\n    assertEquals(1, ret[3]);\r\n}"
        }
      },
      {
        "code": "when(metaData.getColumnLabel(1)).thenReturn(\"three\");",
        "line": 117,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testMapColumnsToPropertiesWithOutUnderscores",
          "methodAnnotations": [
            "SuppressWarnings",
            "Test"
          ],
          "methodRawCode": "// test code\r\n@SuppressWarnings(\"boxing\")\r\n@Test\r\npublic void testMapColumnsToPropertiesWithOutUnderscores() throws Exception {\r\n    when(metaData.getColumnCount()).thenReturn(3);\r\n    when(metaData.getColumnLabel(1)).thenReturn(\"three\");\r\n    when(metaData.getColumnLabel(2)).thenReturn(\"one\");\r\n    when(metaData.getColumnLabel(3)).thenReturn(\"two\");\r\n    final int[] ret = processor.mapColumnsToProperties(metaData, propDescriptors);\r\n    assertNotNull(ret);\r\n    assertEquals(4, ret.length);\r\n    assertEquals(-1, ret[0]);\r\n    assertEquals(2, ret[1]);\r\n    assertEquals(0, ret[2]);\r\n    assertEquals(1, ret[3]);\r\n}"
        }
      },
      {
        "code": "when(metaData.getColumnLabel(2)).thenReturn(\"one\");",
        "line": 118,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testMapColumnsToPropertiesWithOutUnderscores",
          "methodAnnotations": [
            "SuppressWarnings",
            "Test"
          ],
          "methodRawCode": "// test code\r\n@SuppressWarnings(\"boxing\")\r\n@Test\r\npublic void testMapColumnsToPropertiesWithOutUnderscores() throws Exception {\r\n    when(metaData.getColumnCount()).thenReturn(3);\r\n    when(metaData.getColumnLabel(1)).thenReturn(\"three\");\r\n    when(metaData.getColumnLabel(2)).thenReturn(\"one\");\r\n    when(metaData.getColumnLabel(3)).thenReturn(\"two\");\r\n    final int[] ret = processor.mapColumnsToProperties(metaData, propDescriptors);\r\n    assertNotNull(ret);\r\n    assertEquals(4, ret.length);\r\n    assertEquals(-1, ret[0]);\r\n    assertEquals(2, ret[1]);\r\n    assertEquals(0, ret[2]);\r\n    assertEquals(1, ret[3]);\r\n}"
        }
      },
      {
        "code": "when(metaData.getColumnLabel(3)).thenReturn(\"two\");",
        "line": 119,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testMapColumnsToPropertiesWithOutUnderscores",
          "methodAnnotations": [
            "SuppressWarnings",
            "Test"
          ],
          "methodRawCode": "// test code\r\n@SuppressWarnings(\"boxing\")\r\n@Test\r\npublic void testMapColumnsToPropertiesWithOutUnderscores() throws Exception {\r\n    when(metaData.getColumnCount()).thenReturn(3);\r\n    when(metaData.getColumnLabel(1)).thenReturn(\"three\");\r\n    when(metaData.getColumnLabel(2)).thenReturn(\"one\");\r\n    when(metaData.getColumnLabel(3)).thenReturn(\"two\");\r\n    final int[] ret = processor.mapColumnsToProperties(metaData, propDescriptors);\r\n    assertNotNull(ret);\r\n    assertEquals(4, ret.length);\r\n    assertEquals(-1, ret[0]);\r\n    assertEquals(2, ret[1]);\r\n    assertEquals(0, ret[2]);\r\n    assertEquals(1, ret[3]);\r\n}"
        }
      },
      {
        "code": "when(metaData.getColumnCount()).thenReturn(3);",
        "line": 134,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testMapColumnsToPropertiesWithSpaces",
          "methodAnnotations": [
            "SuppressWarnings",
            "Test"
          ],
          "methodRawCode": "// test code\r\n@SuppressWarnings(\"boxing\")\r\n@Test\r\npublic void testMapColumnsToPropertiesWithSpaces() throws Exception {\r\n    when(metaData.getColumnCount()).thenReturn(3);\r\n    when(metaData.getColumnLabel(1)).thenReturn(\"th ree\");\r\n    when(metaData.getColumnLabel(2)).thenReturn(\"o n e\");\r\n    when(metaData.getColumnLabel(3)).thenReturn(\"t wo\");\r\n    final int[] ret = processor.mapColumnsToProperties(metaData, propDescriptors);\r\n    assertNotNull(ret);\r\n    assertEquals(4, ret.length);\r\n    assertEquals(-1, ret[0]);\r\n    assertEquals(2, ret[1]);\r\n    assertEquals(0, ret[2]);\r\n    assertEquals(1, ret[3]);\r\n}"
        }
      },
      {
        "code": "when(metaData.getColumnLabel(1)).thenReturn(\"th ree\");",
        "line": 136,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testMapColumnsToPropertiesWithSpaces",
          "methodAnnotations": [
            "SuppressWarnings",
            "Test"
          ],
          "methodRawCode": "// test code\r\n@SuppressWarnings(\"boxing\")\r\n@Test\r\npublic void testMapColumnsToPropertiesWithSpaces() throws Exception {\r\n    when(metaData.getColumnCount()).thenReturn(3);\r\n    when(metaData.getColumnLabel(1)).thenReturn(\"th ree\");\r\n    when(metaData.getColumnLabel(2)).thenReturn(\"o n e\");\r\n    when(metaData.getColumnLabel(3)).thenReturn(\"t wo\");\r\n    final int[] ret = processor.mapColumnsToProperties(metaData, propDescriptors);\r\n    assertNotNull(ret);\r\n    assertEquals(4, ret.length);\r\n    assertEquals(-1, ret[0]);\r\n    assertEquals(2, ret[1]);\r\n    assertEquals(0, ret[2]);\r\n    assertEquals(1, ret[3]);\r\n}"
        }
      },
      {
        "code": "when(metaData.getColumnLabel(2)).thenReturn(\"o n e\");",
        "line": 137,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testMapColumnsToPropertiesWithSpaces",
          "methodAnnotations": [
            "SuppressWarnings",
            "Test"
          ],
          "methodRawCode": "// test code\r\n@SuppressWarnings(\"boxing\")\r\n@Test\r\npublic void testMapColumnsToPropertiesWithSpaces() throws Exception {\r\n    when(metaData.getColumnCount()).thenReturn(3);\r\n    when(metaData.getColumnLabel(1)).thenReturn(\"th ree\");\r\n    when(metaData.getColumnLabel(2)).thenReturn(\"o n e\");\r\n    when(metaData.getColumnLabel(3)).thenReturn(\"t wo\");\r\n    final int[] ret = processor.mapColumnsToProperties(metaData, propDescriptors);\r\n    assertNotNull(ret);\r\n    assertEquals(4, ret.length);\r\n    assertEquals(-1, ret[0]);\r\n    assertEquals(2, ret[1]);\r\n    assertEquals(0, ret[2]);\r\n    assertEquals(1, ret[3]);\r\n}"
        }
      },
      {
        "code": "when(metaData.getColumnLabel(3)).thenReturn(\"t wo\");",
        "line": 138,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testMapColumnsToPropertiesWithSpaces",
          "methodAnnotations": [
            "SuppressWarnings",
            "Test"
          ],
          "methodRawCode": "// test code\r\n@SuppressWarnings(\"boxing\")\r\n@Test\r\npublic void testMapColumnsToPropertiesWithSpaces() throws Exception {\r\n    when(metaData.getColumnCount()).thenReturn(3);\r\n    when(metaData.getColumnLabel(1)).thenReturn(\"th ree\");\r\n    when(metaData.getColumnLabel(2)).thenReturn(\"o n e\");\r\n    when(metaData.getColumnLabel(3)).thenReturn(\"t wo\");\r\n    final int[] ret = processor.mapColumnsToProperties(metaData, propDescriptors);\r\n    assertNotNull(ret);\r\n    assertEquals(4, ret.length);\r\n    assertEquals(-1, ret[0]);\r\n    assertEquals(2, ret[1]);\r\n    assertEquals(0, ret[2]);\r\n    assertEquals(1, ret[3]);\r\n}"
        }
      },
      {
        "code": "when(metaData.getColumnCount()).thenReturn(3);",
        "line": 153,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testMapColumnsToPropertiesWithUnderscores",
          "methodAnnotations": [
            "SuppressWarnings",
            "Test"
          ],
          "methodRawCode": "// test code\r\n@SuppressWarnings(\"boxing\")\r\n@Test\r\npublic void testMapColumnsToPropertiesWithUnderscores() throws Exception {\r\n    when(metaData.getColumnCount()).thenReturn(3);\r\n    when(metaData.getColumnLabel(1)).thenReturn(\"t_h_r_e_e\");\r\n    when(metaData.getColumnLabel(2)).thenReturn(\"o_n_e\");\r\n    when(metaData.getColumnLabel(3)).thenReturn(\"t_w_o\");\r\n    final int[] ret = processor.mapColumnsToProperties(metaData, propDescriptors);\r\n    assertNotNull(ret);\r\n    assertEquals(4, ret.length);\r\n    assertEquals(-1, ret[0]);\r\n    assertEquals(2, ret[1]);\r\n    assertEquals(0, ret[2]);\r\n    assertEquals(1, ret[3]);\r\n}"
        }
      },
      {
        "code": "when(metaData.getColumnLabel(1)).thenReturn(\"t_h_r_e_e\");",
        "line": 155,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testMapColumnsToPropertiesWithUnderscores",
          "methodAnnotations": [
            "SuppressWarnings",
            "Test"
          ],
          "methodRawCode": "// test code\r\n@SuppressWarnings(\"boxing\")\r\n@Test\r\npublic void testMapColumnsToPropertiesWithUnderscores() throws Exception {\r\n    when(metaData.getColumnCount()).thenReturn(3);\r\n    when(metaData.getColumnLabel(1)).thenReturn(\"t_h_r_e_e\");\r\n    when(metaData.getColumnLabel(2)).thenReturn(\"o_n_e\");\r\n    when(metaData.getColumnLabel(3)).thenReturn(\"t_w_o\");\r\n    final int[] ret = processor.mapColumnsToProperties(metaData, propDescriptors);\r\n    assertNotNull(ret);\r\n    assertEquals(4, ret.length);\r\n    assertEquals(-1, ret[0]);\r\n    assertEquals(2, ret[1]);\r\n    assertEquals(0, ret[2]);\r\n    assertEquals(1, ret[3]);\r\n}"
        }
      },
      {
        "code": "when(metaData.getColumnLabel(2)).thenReturn(\"o_n_e\");",
        "line": 156,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testMapColumnsToPropertiesWithUnderscores",
          "methodAnnotations": [
            "SuppressWarnings",
            "Test"
          ],
          "methodRawCode": "// test code\r\n@SuppressWarnings(\"boxing\")\r\n@Test\r\npublic void testMapColumnsToPropertiesWithUnderscores() throws Exception {\r\n    when(metaData.getColumnCount()).thenReturn(3);\r\n    when(metaData.getColumnLabel(1)).thenReturn(\"t_h_r_e_e\");\r\n    when(metaData.getColumnLabel(2)).thenReturn(\"o_n_e\");\r\n    when(metaData.getColumnLabel(3)).thenReturn(\"t_w_o\");\r\n    final int[] ret = processor.mapColumnsToProperties(metaData, propDescriptors);\r\n    assertNotNull(ret);\r\n    assertEquals(4, ret.length);\r\n    assertEquals(-1, ret[0]);\r\n    assertEquals(2, ret[1]);\r\n    assertEquals(0, ret[2]);\r\n    assertEquals(1, ret[3]);\r\n}"
        }
      },
      {
        "code": "when(metaData.getColumnLabel(3)).thenReturn(\"t_w_o\");",
        "line": 157,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testMapColumnsToPropertiesWithUnderscores",
          "methodAnnotations": [
            "SuppressWarnings",
            "Test"
          ],
          "methodRawCode": "// test code\r\n@SuppressWarnings(\"boxing\")\r\n@Test\r\npublic void testMapColumnsToPropertiesWithUnderscores() throws Exception {\r\n    when(metaData.getColumnCount()).thenReturn(3);\r\n    when(metaData.getColumnLabel(1)).thenReturn(\"t_h_r_e_e\");\r\n    when(metaData.getColumnLabel(2)).thenReturn(\"o_n_e\");\r\n    when(metaData.getColumnLabel(3)).thenReturn(\"t_w_o\");\r\n    final int[] ret = processor.mapColumnsToProperties(metaData, propDescriptors);\r\n    assertNotNull(ret);\r\n    assertEquals(4, ret.length);\r\n    assertEquals(-1, ret[0]);\r\n    assertEquals(2, ret[1]);\r\n    assertEquals(0, ret[2]);\r\n    assertEquals(1, ret[3]);\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 42,
    "variableName": "rs",
    "variableType": "ResultSet",
    "isGlobal": true,
    "mockPatternLevel": 1,
    "classContext": {
      "packageName": "org.apache.commons.dbutils.handlers",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\handlers\\BeanMapHandlerTest.java",
      "className": "BeanMapHandlerTest"
    },
    "statements": [
      {
        "code": "@Mock\r\nprivate ResultSet rs;",
        "line": 40,
        "type": "CREATION",
        "locate": "Attribute",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "FieldDeclaration",
          "methodAnnotations": [
            "Mock"
          ]
        }
      },
      {
        "code": "when(Boolean.valueOf(rs.next())).thenReturn(Boolean.TRUE, Boolean.FALSE);",
        "line": 51,
        "type": "REFERENCE",
        "locate": "@Before",
        "isMockRelated": false,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(Boolean.valueOf(rs.next())).thenReturn(Boolean.TRUE, Boolean.FALSE);\r\n    when(rs.getObject(1)).thenReturn(Long.valueOf(23L));\r\n    when(rs.getObject(2)).thenReturn(Long.valueOf(23L));\r\n    when(rs.getObject(\"id\")).thenReturn(Long.valueOf(23L));\r\n    when(rs.getMetaData()).thenReturn(rsmd);\r\n    when(rp.toBean(rs, TestBean.class)).thenReturn(new TestBean());\r\n}"
        }
      },
      {
        "code": "when(rs.getObject(1)).thenReturn(Long.valueOf(23L));",
        "line": 52,
        "type": "STUBBING",
        "locate": "@Before",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(Boolean.valueOf(rs.next())).thenReturn(Boolean.TRUE, Boolean.FALSE);\r\n    when(rs.getObject(1)).thenReturn(Long.valueOf(23L));\r\n    when(rs.getObject(2)).thenReturn(Long.valueOf(23L));\r\n    when(rs.getObject(\"id\")).thenReturn(Long.valueOf(23L));\r\n    when(rs.getMetaData()).thenReturn(rsmd);\r\n    when(rp.toBean(rs, TestBean.class)).thenReturn(new TestBean());\r\n}"
        }
      },
      {
        "code": "when(rs.getObject(2)).thenReturn(Long.valueOf(23L));",
        "line": 53,
        "type": "STUBBING",
        "locate": "@Before",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(Boolean.valueOf(rs.next())).thenReturn(Boolean.TRUE, Boolean.FALSE);\r\n    when(rs.getObject(1)).thenReturn(Long.valueOf(23L));\r\n    when(rs.getObject(2)).thenReturn(Long.valueOf(23L));\r\n    when(rs.getObject(\"id\")).thenReturn(Long.valueOf(23L));\r\n    when(rs.getMetaData()).thenReturn(rsmd);\r\n    when(rp.toBean(rs, TestBean.class)).thenReturn(new TestBean());\r\n}"
        }
      },
      {
        "code": "when(rs.getObject(\"id\")).thenReturn(Long.valueOf(23L));",
        "line": 54,
        "type": "STUBBING",
        "locate": "@Before",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(Boolean.valueOf(rs.next())).thenReturn(Boolean.TRUE, Boolean.FALSE);\r\n    when(rs.getObject(1)).thenReturn(Long.valueOf(23L));\r\n    when(rs.getObject(2)).thenReturn(Long.valueOf(23L));\r\n    when(rs.getObject(\"id\")).thenReturn(Long.valueOf(23L));\r\n    when(rs.getMetaData()).thenReturn(rsmd);\r\n    when(rp.toBean(rs, TestBean.class)).thenReturn(new TestBean());\r\n}"
        }
      },
      {
        "code": "when(rs.getMetaData()).thenReturn(rsmd);",
        "line": 55,
        "type": "STUBBING",
        "locate": "@Before",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(Boolean.valueOf(rs.next())).thenReturn(Boolean.TRUE, Boolean.FALSE);\r\n    when(rs.getObject(1)).thenReturn(Long.valueOf(23L));\r\n    when(rs.getObject(2)).thenReturn(Long.valueOf(23L));\r\n    when(rs.getObject(\"id\")).thenReturn(Long.valueOf(23L));\r\n    when(rs.getMetaData()).thenReturn(rsmd);\r\n    when(rp.toBean(rs, TestBean.class)).thenReturn(new TestBean());\r\n}"
        }
      },
      {
        "code": "when(rp.toBean(rs, TestBean.class)).thenReturn(new TestBean());",
        "line": 56,
        "type": "REFERENCE",
        "locate": "@Before",
        "isMockRelated": false,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(Boolean.valueOf(rs.next())).thenReturn(Boolean.TRUE, Boolean.FALSE);\r\n    when(rs.getObject(1)).thenReturn(Long.valueOf(23L));\r\n    when(rs.getObject(2)).thenReturn(Long.valueOf(23L));\r\n    when(rs.getObject(\"id\")).thenReturn(Long.valueOf(23L));\r\n    when(rs.getMetaData()).thenReturn(rsmd);\r\n    when(rp.toBean(rs, TestBean.class)).thenReturn(new TestBean());\r\n}"
        }
      },
      {
        "code": "when(Boolean.valueOf(rs.next())).thenReturn(Boolean.FALSE);",
        "line": 85,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testEmptyResultSet",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testEmptyResultSet() throws Exception {\r\n    when(Boolean.valueOf(rs.next())).thenReturn(Boolean.FALSE);\r\n    bmh = new BeanMapHandler<>(TestBean.class);\r\n    res = bmh.handle(rs);\r\n    assertNull(res.get(Long.valueOf(23L)));\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 43,
    "variableName": "rsmd",
    "variableType": "ResultSetMetaData",
    "isGlobal": true,
    "mockPatternLevel": 1,
    "classContext": {
      "packageName": "org.apache.commons.dbutils.handlers",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\handlers\\BeanMapHandlerTest.java",
      "className": "BeanMapHandlerTest"
    },
    "statements": [
      {
        "code": "@Mock\r\nprivate ResultSetMetaData rsmd;",
        "line": 41,
        "type": "CREATION",
        "locate": "Attribute",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "FieldDeclaration",
          "methodAnnotations": [
            "Mock"
          ]
        }
      },
      {
        "code": "when(rs.getMetaData()).thenReturn(rsmd);",
        "line": 55,
        "type": "REFERENCE",
        "locate": "@Before",
        "isMockRelated": false,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(Boolean.valueOf(rs.next())).thenReturn(Boolean.TRUE, Boolean.FALSE);\r\n    when(rs.getObject(1)).thenReturn(Long.valueOf(23L));\r\n    when(rs.getObject(2)).thenReturn(Long.valueOf(23L));\r\n    when(rs.getObject(\"id\")).thenReturn(Long.valueOf(23L));\r\n    when(rs.getMetaData()).thenReturn(rsmd);\r\n    when(rp.toBean(rs, TestBean.class)).thenReturn(new TestBean());\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 44,
    "variableName": "rp",
    "variableType": "RowProcessor",
    "isGlobal": true,
    "mockPatternLevel": 1,
    "classContext": {
      "packageName": "org.apache.commons.dbutils.handlers",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\handlers\\BeanMapHandlerTest.java",
      "className": "BeanMapHandlerTest"
    },
    "statements": [
      {
        "code": "@Mock\r\nprivate RowProcessor rp;",
        "line": 42,
        "type": "CREATION",
        "locate": "Attribute",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "FieldDeclaration",
          "methodAnnotations": [
            "Mock"
          ]
        }
      },
      {
        "code": "when(rp.toBean(rs, TestBean.class)).thenReturn(new TestBean());",
        "line": 56,
        "type": "STUBBING",
        "locate": "@Before",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(Boolean.valueOf(rs.next())).thenReturn(Boolean.TRUE, Boolean.FALSE);\r\n    when(rs.getObject(1)).thenReturn(Long.valueOf(23L));\r\n    when(rs.getObject(2)).thenReturn(Long.valueOf(23L));\r\n    when(rs.getObject(\"id\")).thenReturn(Long.valueOf(23L));\r\n    when(rs.getMetaData()).thenReturn(rsmd);\r\n    when(rp.toBean(rs, TestBean.class)).thenReturn(new TestBean());\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 45,
    "variableName": "rs",
    "variableType": "ResultSet",
    "isGlobal": true,
    "mockPatternLevel": 1,
    "classContext": {
      "packageName": "org.apache.commons.dbutils.handlers.columns",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\handlers\\columns\\AbstractTestColumnHandler.java",
      "className": "AbstractTestColumnHandler"
    },
    "statements": [
      {
        "code": "@Mock\r\nprotected ResultSet rs;",
        "line": 30,
        "type": "CREATION",
        "locate": "Attribute",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "FieldDeclaration",
          "methodAnnotations": [
            "Mock"
          ]
        }
      }
    ]
  },
  {
    "rawMockObjectId": 46,
    "variableName": "sqlxml",
    "variableType": "SQLXML",
    "isGlobal": true,
    "mockPatternLevel": 1,
    "classContext": {
      "packageName": "org.apache.commons.dbutils.handlers.columns",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\handlers\\columns\\SQLXMLColumnHandlerTest.java",
      "className": "SQLXMLColumnHandlerTest"
    },
    "statements": [
      {
        "code": "@Mock\r\nprivate SQLXML sqlxml;",
        "line": 32,
        "type": "CREATION",
        "locate": "Attribute",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "FieldDeclaration",
          "methodAnnotations": [
            "Mock"
          ]
        }
      },
      {
        "code": "when(rs.getSQLXML(1)).thenReturn(sqlxml);",
        "line": 42,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testApplyType",
          "methodAnnotations": [
            "Override",
            "Test"
          ],
          "methodRawCode": "@Override\r\n@Test\r\npublic void testApplyType() throws Exception {\r\n    when(rs.getSQLXML(1)).thenReturn(sqlxml);\r\n    assertTrue(SQLXML.class.isAssignableFrom(handler.apply(rs, 1).getClass()));\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 47,
    "variableName": "mockProc",
    "variableType": "RowProcessor",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils.handlers",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\handlers\\KeyedHandlerTest.java"
    },
    "statements": [
      {
        "code": "final RowProcessor mockProc = mock(RowProcessor.class);",
        "line": 106,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testInjectedRowProcess",
          "methodAnnotations": [],
          "methodRawCode": "public void testInjectedRowProcess() throws Exception {\r\n    final RowProcessor mockProc = mock(RowProcessor.class);\r\n    final ResultSetHandler<Map<String, Map<String, Object>>> h = new KeyedHandler<>(mockProc);\r\n    final Map<String, Map<String, Object>> results = h.handle(this.rs);\r\n    assertNotNull(results);\r\n    assertEquals(ROWS, results.size());\r\n    Map<String, Object> row = null;\r\n    for (final Entry<String, Map<String, Object>> entry : results.entrySet()) {\r\n        row = entry.getValue();\r\n        assertNotNull(row);\r\n        assertTrue(row.isEmpty());\r\n        assertEquals(0, row.size());\r\n    }\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 48,
    "variableName": "stmt",
    "variableType": "CallableStatement",
    "isGlobal": true,
    "mockPatternLevel": 2,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\OutParameterTest.java",
      "className": "OutParameterTest"
    },
    "statements": [
      {
        "code": "@Mock\r\nCallableStatement stmt;",
        "line": 41,
        "type": "CREATION",
        "locate": "Attribute",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "FieldDeclaration",
          "methodAnnotations": [
            "Mock"
          ]
        }
      },
      {
        "code": "parameter.register(stmt, INDEX);",
        "line": 52,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testRegister",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testRegister() throws Exception {\r\n    parameter.register(stmt, INDEX);\r\n    verify(stmt, times(1)).registerOutParameter(INDEX, Types.INTEGER);\r\n    verify(stmt, times(0)).setObject(eq(INDEX), any(Number.class));\r\n    parameter.setValue(VALUE);\r\n    parameter.register(stmt, INDEX);\r\n    verify(stmt, times(2)).registerOutParameter(INDEX, Types.INTEGER);\r\n    verify(stmt, times(1)).setObject(INDEX, VALUE);\r\n}"
        }
      },
      {
        "code": "verify(stmt, times(1)).registerOutParameter(INDEX, Types.INTEGER);",
        "line": 53,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testRegister",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testRegister() throws Exception {\r\n    parameter.register(stmt, INDEX);\r\n    verify(stmt, times(1)).registerOutParameter(INDEX, Types.INTEGER);\r\n    verify(stmt, times(0)).setObject(eq(INDEX), any(Number.class));\r\n    parameter.setValue(VALUE);\r\n    parameter.register(stmt, INDEX);\r\n    verify(stmt, times(2)).registerOutParameter(INDEX, Types.INTEGER);\r\n    verify(stmt, times(1)).setObject(INDEX, VALUE);\r\n}"
        }
      },
      {
        "code": "verify(stmt, times(0)).setObject(eq(INDEX), any(Number.class));",
        "line": 54,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testRegister",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testRegister() throws Exception {\r\n    parameter.register(stmt, INDEX);\r\n    verify(stmt, times(1)).registerOutParameter(INDEX, Types.INTEGER);\r\n    verify(stmt, times(0)).setObject(eq(INDEX), any(Number.class));\r\n    parameter.setValue(VALUE);\r\n    parameter.register(stmt, INDEX);\r\n    verify(stmt, times(2)).registerOutParameter(INDEX, Types.INTEGER);\r\n    verify(stmt, times(1)).setObject(INDEX, VALUE);\r\n}"
        }
      },
      {
        "code": "parameter.register(stmt, INDEX);",
        "line": 57,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testRegister",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testRegister() throws Exception {\r\n    parameter.register(stmt, INDEX);\r\n    verify(stmt, times(1)).registerOutParameter(INDEX, Types.INTEGER);\r\n    verify(stmt, times(0)).setObject(eq(INDEX), any(Number.class));\r\n    parameter.setValue(VALUE);\r\n    parameter.register(stmt, INDEX);\r\n    verify(stmt, times(2)).registerOutParameter(INDEX, Types.INTEGER);\r\n    verify(stmt, times(1)).setObject(INDEX, VALUE);\r\n}"
        }
      },
      {
        "code": "verify(stmt, times(2)).registerOutParameter(INDEX, Types.INTEGER);",
        "line": 58,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testRegister",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testRegister() throws Exception {\r\n    parameter.register(stmt, INDEX);\r\n    verify(stmt, times(1)).registerOutParameter(INDEX, Types.INTEGER);\r\n    verify(stmt, times(0)).setObject(eq(INDEX), any(Number.class));\r\n    parameter.setValue(VALUE);\r\n    parameter.register(stmt, INDEX);\r\n    verify(stmt, times(2)).registerOutParameter(INDEX, Types.INTEGER);\r\n    verify(stmt, times(1)).setObject(INDEX, VALUE);\r\n}"
        }
      },
      {
        "code": "verify(stmt, times(1)).setObject(INDEX, VALUE);",
        "line": 59,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testRegister",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testRegister() throws Exception {\r\n    parameter.register(stmt, INDEX);\r\n    verify(stmt, times(1)).registerOutParameter(INDEX, Types.INTEGER);\r\n    verify(stmt, times(0)).setObject(eq(INDEX), any(Number.class));\r\n    parameter.setValue(VALUE);\r\n    parameter.register(stmt, INDEX);\r\n    verify(stmt, times(2)).registerOutParameter(INDEX, Types.INTEGER);\r\n    verify(stmt, times(1)).setObject(INDEX, VALUE);\r\n}"
        }
      },
      {
        "code": "parameter.register(stmt, INDEX);",
        "line": 65,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testRegisterAlternateConstructor",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testRegisterAlternateConstructor() throws Exception {\r\n    parameter = new OutParameter<>(Types.INTEGER, Number.class, VALUE);\r\n    parameter.register(stmt, INDEX);\r\n    verify(stmt, times(1)).registerOutParameter(INDEX, Types.INTEGER);\r\n    verify(stmt, times(1)).setObject(INDEX, VALUE);\r\n}"
        }
      },
      {
        "code": "verify(stmt, times(1)).registerOutParameter(INDEX, Types.INTEGER);",
        "line": 66,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testRegisterAlternateConstructor",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testRegisterAlternateConstructor() throws Exception {\r\n    parameter = new OutParameter<>(Types.INTEGER, Number.class, VALUE);\r\n    parameter.register(stmt, INDEX);\r\n    verify(stmt, times(1)).registerOutParameter(INDEX, Types.INTEGER);\r\n    verify(stmt, times(1)).setObject(INDEX, VALUE);\r\n}"
        }
      },
      {
        "code": "verify(stmt, times(1)).setObject(INDEX, VALUE);",
        "line": 67,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testRegisterAlternateConstructor",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testRegisterAlternateConstructor() throws Exception {\r\n    parameter = new OutParameter<>(Types.INTEGER, Number.class, VALUE);\r\n    parameter.register(stmt, INDEX);\r\n    verify(stmt, times(1)).registerOutParameter(INDEX, Types.INTEGER);\r\n    verify(stmt, times(1)).setObject(INDEX, VALUE);\r\n}"
        }
      },
      {
        "code": "when(stmt.getObject(INDEX)).thenReturn(VALUE);",
        "line": 72,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testSetValue",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testSetValue() throws Exception {\r\n    when(stmt.getObject(INDEX)).thenReturn(VALUE);\r\n    parameter.setValue(stmt, INDEX);\r\n    assertEquals(VALUE, parameter.getValue());\r\n}"
        }
      },
      {
        "code": "parameter.setValue(stmt, INDEX);",
        "line": 74,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testSetValue",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testSetValue() throws Exception {\r\n    when(stmt.getObject(INDEX)).thenReturn(VALUE);\r\n    parameter.setValue(stmt, INDEX);\r\n    assertEquals(VALUE, parameter.getValue());\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 49,
    "variableName": "dataSource",
    "variableType": "DataSource",
    "isGlobal": true,
    "mockPatternLevel": 2,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\QueryRunnerTest.java",
      "className": "QueryRunnerTest"
    },
    "statements": [
      {
        "code": "@Mock\r\nDataSource dataSource;",
        "line": 75,
        "type": "CREATION",
        "locate": "Attribute",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "FieldDeclaration",
          "methodAnnotations": [
            "Mock"
          ]
        }
      },
      {
        "code": "when(dataSource.getConnection()).thenReturn(conn);",
        "line": 594,
        "type": "STUBBING",
        "locate": "@Before",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(conn.prepareCall(any(String.class))).thenReturn(call);\r\n    when(call.getParameterMetaData()).thenReturn(meta);\r\n    when(call.getResultSet()).thenReturn(results);\r\n    when(call.getMoreResults()).thenReturn(false);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new QueryRunner(dataSource);\r\n}"
        }
      },
      {
        "code": "when(dataSource.getConnection()).thenReturn(null);",
        "line": 867,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testNullConnectionBatch",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test(expected = SQLException.class)\r\npublic void testNullConnectionBatch() throws Exception {\r\n    final String[][] params = { { \"unit\", \"unit\" }, { \"test\", \"test\" } };\r\n    when(dataSource.getConnection()).thenReturn(null);\r\n    runner.batch(\"select * from blah where ? = ?\", params);\r\n}"
        }
      },
      {
        "code": "when(dataSource.getConnection()).thenReturn(null);",
        "line": 874,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testNullConnectionExecute",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test(expected = SQLException.class)\r\npublic void testNullConnectionExecute() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(null);\r\n    runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n}"
        }
      },
      {
        "code": "when(dataSource.getConnection()).thenReturn(null);",
        "line": 881,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testNullConnectionExecuteWithResultSet",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test(expected = SQLException.class)\r\npublic void testNullConnectionExecuteWithResultSet() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(null);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n}"
        }
      },
      {
        "code": "when(dataSource.getConnection()).thenReturn(null);",
        "line": 888,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testNullConnectionQuery",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test(expected = SQLException.class)\r\npublic void testNullConnectionQuery() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(null);\r\n    runner.query(\"select * from blah where ? = ?\", handler, \"unit\", \"test\");\r\n}"
        }
      },
      {
        "code": "when(dataSource.getConnection()).thenReturn(null);",
        "line": 895,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testNullConnectionUpdate",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test(expected = SQLException.class)\r\npublic void testNullConnectionUpdate() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(null);\r\n    runner.update(\"select * from blah where ? = ?\", \"unit\", \"test\");\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 50,
    "variableName": "conn",
    "variableType": "Connection",
    "isGlobal": true,
    "mockPatternLevel": 2,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\QueryRunnerTest.java",
      "className": "QueryRunnerTest"
    },
    "statements": [
      {
        "code": "@Mock\r\nConnection conn;",
        "line": 76,
        "type": "CREATION",
        "locate": "Attribute",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "FieldDeclaration",
          "methodAnnotations": [
            "Mock"
          ]
        }
      },
      {
        "code": "runner.batch(conn, \"select * from blah where ? = ?\", params);",
        "line": 150,
        "type": "REFERENCE",
        "locate": "Other Methods",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodBatch",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodBatch(final Connection conn, final Object[][] params) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.batch(conn, \"select * from blah where ? = ?\", params);\r\n    verify(prepStmt, times(1)).getParameterMetaData();\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.batch(Connection, String, Object[][]) does not close connections\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection, since QueryRunner.batch(Connection, String, Object[][]) does not close connections\r\nverify(conn, times(0)).close();",
        "line": 156,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodBatch",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodBatch(final Connection conn, final Object[][] params) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.batch(conn, \"select * from blah where ? = ?\", params);\r\n    verify(prepStmt, times(1)).getParameterMetaData();\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.batch(Connection, String, Object[][]) does not close connections\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the connection\r\nverify(conn, times(1)).close();",
        "line": 171,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodBatch",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodBatch(final Object[][] params, final boolean pmdCheck) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.batch(\"select * from blah where ? = ?\", params);\r\n    verify(prepStmt, times(pmdCheck ? 1 : 0)).getParameterMetaData();\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection\r\nverify(conn, times(1)).close();",
        "line": 185,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection\r\nverify(conn, times(2)).close();",
        "line": 195,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection\r\nverify(conn, times(3)).close();",
        "line": 209,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection\r\nverify(conn, times(4)).close();",
        "line": 222,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection\r\nverify(conn, times(5)).close();",
        "line": 239,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection\r\nverify(conn, times(0)).close();",
        "line": 257,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection\r\nverify(conn, times(0)).close();",
        "line": 267,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection\r\nverify(conn, times(0)).close();",
        "line": 281,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection\r\nverify(conn, times(0)).close();",
        "line": 294,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection\r\nverify(conn, times(0)).close();",
        "line": 311,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection\r\nverify(conn, times(1)).close();",
        "line": 323,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection\r\nverify(conn, times(2)).close();",
        "line": 332,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection\r\nverify(conn, times(3)).close();",
        "line": 346,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection\r\nverify(conn, times(4)).close();",
        "line": 359,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection\r\nverify(conn, times(5)).close();",
        "line": 376,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");",
        "line": 383,
        "type": "REFERENCE",
        "locate": "Other Methods",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection\r\nverify(conn, times(0)).close();",
        "line": 388,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "runner.execute(conn, \"{call my_proc()}\", handler);",
        "line": 392,
        "type": "REFERENCE",
        "locate": "Other Methods",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection\r\nverify(conn, times(0)).close();",
        "line": 397,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);",
        "line": 404,
        "type": "REFERENCE",
        "locate": "Other Methods",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection\r\nverify(conn, times(0)).close();",
        "line": 411,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");",
        "line": 417,
        "type": "REFERENCE",
        "locate": "Other Methods",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection\r\nverify(conn, times(0)).close();",
        "line": 424,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);",
        "line": 433,
        "type": "REFERENCE",
        "locate": "Other Methods",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection\r\nverify(conn, times(0)).close();",
        "line": 441,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the connection\r\nverify(conn, times(1)).close();",
        "line": 452,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodQuery() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(sql, handler, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(sql, handler);\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the connection\r\nverify(conn, times(2)).close();",
        "line": 461,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodQuery() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(sql, handler, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(sql, handler);\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n}"
        }
      },
      {
        "code": "runner.query(conn, sql, handler, \"unit\", \"test\");",
        "line": 471,
        "type": "REFERENCE",
        "locate": "Other Methods",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Query test cases\r\n//\r\nprivate void callGoodQuery(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(conn, sql, handler, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.query(Connection, String, ResultSetHandler<T>, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(conn, sql, handler);\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection, since QueryRunner.query(Connection, String, ResultSetHandler<T>, Object...) does not close connections\r\nverify(conn, times(0)).close();",
        "line": 476,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Query test cases\r\n//\r\nprivate void callGoodQuery(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(conn, sql, handler, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.query(Connection, String, ResultSetHandler<T>, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(conn, sql, handler);\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "runner.query(conn, sql, handler);",
        "line": 480,
        "type": "REFERENCE",
        "locate": "Other Methods",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Query test cases\r\n//\r\nprivate void callGoodQuery(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(conn, sql, handler, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.query(Connection, String, ResultSetHandler<T>, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(conn, sql, handler);\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection, see above\r\nverify(conn, times(0)).close();",
        "line": 485,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Query test cases\r\n//\r\nprivate void callGoodQuery(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(conn, sql, handler, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.query(Connection, String, ResultSetHandler<T>, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(conn, sql, handler);\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the connection\r\nverify(conn, times(1)).close();",
        "line": 496,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(sql, \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the connection\r\nverify(conn, times(2)).close();",
        "line": 505,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(sql, \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the connection\r\nverify(conn, times(3)).close();",
        "line": 514,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(sql, \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "runner.update(conn, \"update blah set ? = ?\", \"unit\", \"test\");",
        "line": 522,
        "type": "REFERENCE",
        "locate": "Other Methods",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.update(conn, \"update blah set ? = ?\", \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.update(Connection, String, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    final String sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(conn, \"update blah set unit = ?\", \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection, since QueryRunner.update(Connection, String, Object...) does not close connections\r\nverify(conn, times(0)).close();",
        "line": 526,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.update(conn, \"update blah set ? = ?\", \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.update(Connection, String, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    final String sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(conn, \"update blah set unit = ?\", \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "runner.update(conn, sql);",
        "line": 531,
        "type": "REFERENCE",
        "locate": "Other Methods",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.update(conn, \"update blah set ? = ?\", \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.update(Connection, String, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    final String sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(conn, \"update blah set unit = ?\", \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection, see above\r\nverify(conn, times(0)).close();",
        "line": 535,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.update(conn, \"update blah set ? = ?\", \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.update(Connection, String, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    final String sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(conn, \"update blah set unit = ?\", \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "runner.update(conn, \"update blah set unit = ?\", \"test\");",
        "line": 539,
        "type": "REFERENCE",
        "locate": "Other Methods",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.update(conn, \"update blah set ? = ?\", \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.update(Connection, String, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    final String sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(conn, \"update blah set unit = ?\", \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we do not close the connection, see above\r\nverify(conn, times(0)).close();",
        "line": 543,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.update(conn, \"update blah set ? = ?\", \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.update(Connection, String, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    final String sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(conn, \"update blah set unit = ?\", \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(dataSource.getConnection()).thenReturn(conn);",
        "line": 594,
        "type": "REFERENCE",
        "locate": "@Before",
        "isMockRelated": false,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(conn.prepareCall(any(String.class))).thenReturn(call);\r\n    when(call.getParameterMetaData()).thenReturn(meta);\r\n    when(call.getResultSet()).thenReturn(results);\r\n    when(call.getMoreResults()).thenReturn(false);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new QueryRunner(dataSource);\r\n}"
        }
      },
      {
        "code": "when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);",
        "line": 596,
        "type": "STUBBING",
        "locate": "@Before",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(conn.prepareCall(any(String.class))).thenReturn(call);\r\n    when(call.getParameterMetaData()).thenReturn(meta);\r\n    when(call.getResultSet()).thenReturn(results);\r\n    when(call.getMoreResults()).thenReturn(false);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new QueryRunner(dataSource);\r\n}"
        }
      },
      {
        "code": "when(conn.createStatement()).thenReturn(stmt);",
        "line": 600,
        "type": "STUBBING",
        "locate": "@Before",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(conn.prepareCall(any(String.class))).thenReturn(call);\r\n    when(call.getParameterMetaData()).thenReturn(meta);\r\n    when(call.getResultSet()).thenReturn(results);\r\n    when(call.getMoreResults()).thenReturn(false);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new QueryRunner(dataSource);\r\n}"
        }
      },
      {
        "code": "when(conn.prepareCall(any(String.class))).thenReturn(call);",
        "line": 603,
        "type": "STUBBING",
        "locate": "@Before",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(conn.prepareCall(any(String.class))).thenReturn(call);\r\n    when(call.getParameterMetaData()).thenReturn(meta);\r\n    when(call.getResultSet()).thenReturn(results);\r\n    when(call.getMoreResults()).thenReturn(false);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new QueryRunner(dataSource);\r\n}"
        }
      },
      {
        "code": "// make sure we close the connection\r\nverify(conn, times(1)).close();",
        "line": 675,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testExecuteWithMultipleResultSets",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testExecuteWithMultipleResultSets() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(call.getMoreResults()).thenAnswer(new Answer<Boolean>() {\r\n\r\n        int count = 1;\r\n\r\n        @Override\r\n        public Boolean answer(final InvocationOnMock invocation) {\r\n            return ++count <= 3;\r\n        }\r\n    });\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    final List<Object[]> objects = runner.execute(\"{call my_proc()}\", handler);\r\n    Assert.assertEquals(3, objects.size());\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we close the connection\r\n    verify(conn, times(1)).close();\r\n}"
        }
      },
      {
        "code": "callGoodBatch(conn, params);",
        "line": 710,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodBatchDefaultConstructor",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodBatchDefaultConstructor() throws Exception {\r\n    runner = new QueryRunner();\r\n    final String[][] params = { { \"unit\", \"unit\" }, { \"test\", \"test\" } };\r\n    callGoodBatch(conn, params);\r\n}"
        }
      },
      {
        "code": "when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);",
        "line": 719,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodBatchInsert",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodBatchInsert() throws Exception {\r\n    results = mock(ResultSet.class);\r\n    resultsMeta = mock(ResultSetMetaData.class);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);\r\n    when(prepStmt.getGeneratedKeys()).thenReturn(results);\r\n    when(results.next()).thenReturn(true).thenReturn(true).thenReturn(false);\r\n    final ResultSetHandler<List<Object>> handler = rs -> {\r\n        final List<Object> objects = new ArrayList<>();\r\n        while (rs.next()) {\r\n            objects.add(new Object());\r\n        }\r\n        return objects;\r\n    };\r\n    final Object[][] params = new Object[2][2];\r\n    params[0][0] = \"Test\";\r\n    params[0][1] = \"Blah\";\r\n    params[1][0] = \"Test2\";\r\n    params[1][1] = \"Blah2\";\r\n    final List<Object> generatedKeys = runner.insertBatch(\"INSERT INTO blah(col1, col2) VALUES(?,?)\", handler, params);\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    Assert.assertEquals(2, generatedKeys.size());\r\n}"
        }
      },
      {
        "code": "// make sure we closed the connection\r\nverify(conn, times(1)).close();",
        "line": 743,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodBatchInsert",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodBatchInsert() throws Exception {\r\n    results = mock(ResultSet.class);\r\n    resultsMeta = mock(ResultSetMetaData.class);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);\r\n    when(prepStmt.getGeneratedKeys()).thenReturn(results);\r\n    when(results.next()).thenReturn(true).thenReturn(true).thenReturn(false);\r\n    final ResultSetHandler<List<Object>> handler = rs -> {\r\n        final List<Object> objects = new ArrayList<>();\r\n        while (rs.next()) {\r\n            objects.add(new Object());\r\n        }\r\n        return objects;\r\n    };\r\n    final Object[][] params = new Object[2][2];\r\n    params[0][0] = \"Test\";\r\n    params[0][1] = \"Blah\";\r\n    params[1][0] = \"Test2\";\r\n    params[1][1] = \"Blah2\";\r\n    final List<Object> generatedKeys = runner.insertBatch(\"INSERT INTO blah(col1, col2) VALUES(?,?)\", handler, params);\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    Assert.assertEquals(2, generatedKeys.size());\r\n}"
        }
      },
      {
        "code": "callGoodExecute(conn);",
        "line": 764,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodExecuteDefaultConstructor",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodExecuteDefaultConstructor() throws Exception {\r\n    runner = new QueryRunner();\r\n    callGoodExecute(conn);\r\n}"
        }
      },
      {
        "code": "callGoodExecute(conn);",
        "line": 770,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodExecutePmdTrue",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodExecutePmdTrue() throws Exception {\r\n    runner = new QueryRunner(true);\r\n    callGoodExecute(conn);\r\n}"
        }
      },
      {
        "code": "callGoodExecuteWithResultSet(conn);",
        "line": 781,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodExecuteWithResultSetDefaultConstructor",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodExecuteWithResultSetDefaultConstructor() throws Exception {\r\n    runner = new QueryRunner();\r\n    callGoodExecuteWithResultSet(conn);\r\n}"
        }
      },
      {
        "code": "callGoodExecuteWithResultSet(conn);",
        "line": 787,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodExecuteWithResultSetPmdTrue",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodExecuteWithResultSetPmdTrue() throws Exception {\r\n    runner = new QueryRunner(true);\r\n    callGoodExecuteWithResultSet(conn);\r\n}"
        }
      },
      {
        "code": "when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);",
        "line": 795,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodInsert",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodInsert() throws Exception {\r\n    results = mock(ResultSet.class);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);\r\n    when(prepStmt.getGeneratedKeys()).thenReturn(results);\r\n    when(results.next()).thenReturn(true).thenReturn(false);\r\n    when(results.getObject(1)).thenReturn(1L);\r\n    final Long generatedKey = runner.insert(\"INSERT INTO blah(col1, col2) VALUES(?,?)\", new ScalarHandler<>(), \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    Assert.assertEquals(1L, generatedKey.longValue());\r\n}"
        }
      },
      {
        "code": "// make sure we closed the connection\r\nverify(conn, times(1)).close();",
        "line": 804,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodInsert",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodInsert() throws Exception {\r\n    results = mock(ResultSet.class);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);\r\n    when(prepStmt.getGeneratedKeys()).thenReturn(results);\r\n    when(results.next()).thenReturn(true).thenReturn(false);\r\n    when(results.getObject(1)).thenReturn(1L);\r\n    final Long generatedKey = runner.insert(\"INSERT INTO blah(col1, col2) VALUES(?,?)\", new ScalarHandler<>(), \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    Assert.assertEquals(1L, generatedKey.longValue());\r\n}"
        }
      },
      {
        "code": "callGoodQuery(conn);",
        "line": 817,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodQueryDefaultConstructor",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodQueryDefaultConstructor() throws Exception {\r\n    runner = new QueryRunner();\r\n    callGoodQuery(conn);\r\n}"
        }
      },
      {
        "code": "callGoodQuery(conn);",
        "line": 823,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodQueryPmdTrue",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodQueryPmdTrue() throws Exception {\r\n    runner = new QueryRunner(true);\r\n    callGoodQuery(conn);\r\n}"
        }
      },
      {
        "code": "callGoodUpdate(conn);",
        "line": 834,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodUpdateDefaultConstructor",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodUpdateDefaultConstructor() throws Exception {\r\n    runner = new QueryRunner();\r\n    callGoodUpdate(conn);\r\n}"
        }
      },
      {
        "code": "callGoodUpdate(conn);",
        "line": 840,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodUpdatePmdTrue",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodUpdatePmdTrue() throws Exception {\r\n    runner = new QueryRunner(true);\r\n    callGoodUpdate(conn);\r\n}"
        }
      },
      {
        "code": "queryRunner.prepareStatement(conn, \"select 1\");",
        "line": 964,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testStatementConfiguration",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testStatementConfiguration() throws Exception {\r\n    final StatementConfiguration stmtConfig = new StatementConfiguration(1, 2, 3, 4, 5);\r\n    final QueryRunner queryRunner = new QueryRunner(stmtConfig);\r\n    queryRunner.prepareStatement(conn, \"select 1\");\r\n    verify(prepStmt).setFetchDirection(eq(1));\r\n    verify(prepStmt).setFetchSize(eq(2));\r\n    verify(prepStmt).setMaxFieldSize(eq(3));\r\n    verify(prepStmt).setMaxRows(eq(4));\r\n    verify(prepStmt).setQueryTimeout(eq(5));\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 51,
    "variableName": "prepStmt",
    "variableType": "PreparedStatement",
    "isGlobal": true,
    "mockPatternLevel": 2,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\QueryRunnerTest.java",
      "className": "QueryRunnerTest"
    },
    "statements": [
      {
        "code": "@Mock\r\nPreparedStatement prepStmt;",
        "line": 77,
        "type": "CREATION",
        "locate": "Attribute",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "FieldDeclaration",
          "methodAnnotations": [
            "Mock"
          ]
        }
      },
      {
        "code": "verify(prepStmt, times(1)).getParameterMetaData();",
        "line": 152,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodBatch",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodBatch(final Connection conn, final Object[][] params) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.batch(conn, \"select * from blah where ? = ?\", params);\r\n    verify(prepStmt, times(1)).getParameterMetaData();\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.batch(Connection, String, Object[][]) does not close connections\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(prepStmt, times(2)).addBatch();",
        "line": 153,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodBatch",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodBatch(final Connection conn, final Object[][] params) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.batch(conn, \"select * from blah where ? = ?\", params);\r\n    verify(prepStmt, times(1)).getParameterMetaData();\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.batch(Connection, String, Object[][]) does not close connections\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(prepStmt, times(1)).executeBatch();",
        "line": 154,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodBatch",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodBatch(final Connection conn, final Object[][] params) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.batch(conn, \"select * from blah where ? = ?\", params);\r\n    verify(prepStmt, times(1)).getParameterMetaData();\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.batch(Connection, String, Object[][]) does not close connections\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(prepStmt, times(1)).close();",
        "line": 155,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodBatch",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodBatch(final Connection conn, final Object[][] params) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.batch(conn, \"select * from blah where ? = ?\", params);\r\n    verify(prepStmt, times(1)).getParameterMetaData();\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.batch(Connection, String, Object[][]) does not close connections\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(prepStmt, times(pmdCheck ? 1 : 0)).getParameterMetaData();",
        "line": 167,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodBatch",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodBatch(final Object[][] params, final boolean pmdCheck) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.batch(\"select * from blah where ? = ?\", params);\r\n    verify(prepStmt, times(pmdCheck ? 1 : 0)).getParameterMetaData();\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n}"
        }
      },
      {
        "code": "verify(prepStmt, times(2)).addBatch();",
        "line": 168,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodBatch",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodBatch(final Object[][] params, final boolean pmdCheck) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.batch(\"select * from blah where ? = ?\", params);\r\n    verify(prepStmt, times(pmdCheck ? 1 : 0)).getParameterMetaData();\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n}"
        }
      },
      {
        "code": "verify(prepStmt, times(1)).executeBatch();",
        "line": 169,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodBatch",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodBatch(final Object[][] params, final boolean pmdCheck) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.batch(\"select * from blah where ? = ?\", params);\r\n    verify(prepStmt, times(pmdCheck ? 1 : 0)).getParameterMetaData();\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(prepStmt, times(1)).close();",
        "line": 170,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodBatch",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodBatch(final Object[][] params, final boolean pmdCheck) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.batch(\"select * from blah where ? = ?\", params);\r\n    verify(prepStmt, times(pmdCheck ? 1 : 0)).getParameterMetaData();\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n}"
        }
      },
      {
        "code": "verify(prepStmt, times(1)).executeQuery();",
        "line": 449,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodQuery() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(sql, handler, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(sql, handler);\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(prepStmt, times(1)).close();",
        "line": 451,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodQuery() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(sql, handler, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(sql, handler);\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n}"
        }
      },
      {
        "code": "verify(prepStmt, times(1)).executeQuery();",
        "line": 473,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Query test cases\r\n//\r\nprivate void callGoodQuery(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(conn, sql, handler, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.query(Connection, String, ResultSetHandler<T>, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(conn, sql, handler);\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(prepStmt, times(1)).close();",
        "line": 475,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Query test cases\r\n//\r\nprivate void callGoodQuery(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(conn, sql, handler, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.query(Connection, String, ResultSetHandler<T>, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(conn, sql, handler);\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(prepStmt, times(1)).executeUpdate();",
        "line": 494,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(sql, \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(prepStmt, times(1)).close();",
        "line": 495,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(sql, \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "verify(prepStmt, times(2)).executeUpdate();",
        "line": 512,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(sql, \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(prepStmt, times(2)).close();",
        "line": 513,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(sql, \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "verify(prepStmt, times(1)).executeUpdate();",
        "line": 524,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.update(conn, \"update blah set ? = ?\", \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.update(Connection, String, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    final String sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(conn, \"update blah set unit = ?\", \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(prepStmt, times(1)).close();",
        "line": 525,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.update(conn, \"update blah set ? = ?\", \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.update(Connection, String, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    final String sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(conn, \"update blah set unit = ?\", \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(prepStmt, times(2)).executeUpdate();",
        "line": 541,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.update(conn, \"update blah set ? = ?\", \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.update(Connection, String, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    final String sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(conn, \"update blah set unit = ?\", \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(prepStmt, times(2)).close();",
        "line": 542,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.update(conn, \"update blah set ? = ?\", \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.update(Connection, String, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    final String sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(conn, \"update blah set unit = ?\", \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);",
        "line": 596,
        "type": "REFERENCE",
        "locate": "@Before",
        "isMockRelated": false,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(conn.prepareCall(any(String.class))).thenReturn(call);\r\n    when(call.getParameterMetaData()).thenReturn(meta);\r\n    when(call.getResultSet()).thenReturn(results);\r\n    when(call.getMoreResults()).thenReturn(false);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new QueryRunner(dataSource);\r\n}"
        }
      },
      {
        "code": "when(prepStmt.getParameterMetaData()).thenReturn(meta);",
        "line": 597,
        "type": "STUBBING",
        "locate": "@Before",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(conn.prepareCall(any(String.class))).thenReturn(call);\r\n    when(call.getParameterMetaData()).thenReturn(meta);\r\n    when(call.getResultSet()).thenReturn(results);\r\n    when(call.getMoreResults()).thenReturn(false);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new QueryRunner(dataSource);\r\n}"
        }
      },
      {
        "code": "when(prepStmt.executeQuery()).thenReturn(results);",
        "line": 598,
        "type": "STUBBING",
        "locate": "@Before",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(conn.prepareCall(any(String.class))).thenReturn(call);\r\n    when(call.getParameterMetaData()).thenReturn(meta);\r\n    when(call.getResultSet()).thenReturn(results);\r\n    when(call.getMoreResults()).thenReturn(false);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new QueryRunner(dataSource);\r\n}"
        }
      },
      {
        "code": "doThrow(new SQLException()).when(prepStmt).addBatch();",
        "line": 618,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testAddBatchExceptionOnAdd",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testAddBatchExceptionOnAdd() throws Exception {\r\n    final String[][] params = { { \"unit\", \"unit\" }, { \"test\", \"test\" } };\r\n    doThrow(new SQLException()).when(prepStmt).addBatch();\r\n    callBatchWithException(\"select * from blah where ? = ?\", params);\r\n}"
        }
      },
      {
        "code": "doThrow(new SQLException()).when(prepStmt).executeBatch();",
        "line": 633,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testExecuteBatchExceptionOnExec",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testExecuteBatchExceptionOnExec() throws Exception {\r\n    final String[][] params = { { \"unit\", \"unit\" }, { \"test\", \"test\" } };\r\n    doThrow(new SQLException()).when(prepStmt).executeBatch();\r\n    callBatchWithException(\"select * from blah where ? = ?\", params);\r\n}"
        }
      },
      {
        "code": "doThrow(new SQLException()).when(prepStmt).executeUpdate();",
        "line": 651,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testExecuteUpdateException",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testExecuteUpdateException() throws Exception {\r\n    doThrow(new SQLException()).when(prepStmt).executeUpdate();\r\n    callUpdateWithException(\"unit\", \"test\");\r\n}"
        }
      },
      {
        "code": "runner.fillStatementWithBean(prepStmt, bean, \"a\", \"b\", \"c\");",
        "line": 688,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testFillStatementWithBean",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testFillStatementWithBean() throws Exception {\r\n    final MyBean bean = new MyBean();\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    runner.fillStatementWithBean(prepStmt, bean, \"a\", \"b\", \"c\");\r\n}"
        }
      },
      {
        "code": "runner.fillStatementWithBean(prepStmt, bean, \"a\", \"b\", null);",
        "line": 694,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testFillStatementWithBeanNullNames",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test(expected = NullPointerException.class)\r\npublic void testFillStatementWithBeanNullNames() throws Exception {\r\n    final MyBean bean = new MyBean();\r\n    runner.fillStatementWithBean(prepStmt, bean, \"a\", \"b\", null);\r\n}"
        }
      },
      {
        "code": "when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);",
        "line": 719,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodBatchInsert",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodBatchInsert() throws Exception {\r\n    results = mock(ResultSet.class);\r\n    resultsMeta = mock(ResultSetMetaData.class);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);\r\n    when(prepStmt.getGeneratedKeys()).thenReturn(results);\r\n    when(results.next()).thenReturn(true).thenReturn(true).thenReturn(false);\r\n    final ResultSetHandler<List<Object>> handler = rs -> {\r\n        final List<Object> objects = new ArrayList<>();\r\n        while (rs.next()) {\r\n            objects.add(new Object());\r\n        }\r\n        return objects;\r\n    };\r\n    final Object[][] params = new Object[2][2];\r\n    params[0][0] = \"Test\";\r\n    params[0][1] = \"Blah\";\r\n    params[1][0] = \"Test2\";\r\n    params[1][1] = \"Blah2\";\r\n    final List<Object> generatedKeys = runner.insertBatch(\"INSERT INTO blah(col1, col2) VALUES(?,?)\", handler, params);\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    Assert.assertEquals(2, generatedKeys.size());\r\n}"
        }
      },
      {
        "code": "when(prepStmt.getGeneratedKeys()).thenReturn(results);",
        "line": 720,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodBatchInsert",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodBatchInsert() throws Exception {\r\n    results = mock(ResultSet.class);\r\n    resultsMeta = mock(ResultSetMetaData.class);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);\r\n    when(prepStmt.getGeneratedKeys()).thenReturn(results);\r\n    when(results.next()).thenReturn(true).thenReturn(true).thenReturn(false);\r\n    final ResultSetHandler<List<Object>> handler = rs -> {\r\n        final List<Object> objects = new ArrayList<>();\r\n        while (rs.next()) {\r\n            objects.add(new Object());\r\n        }\r\n        return objects;\r\n    };\r\n    final Object[][] params = new Object[2][2];\r\n    params[0][0] = \"Test\";\r\n    params[0][1] = \"Blah\";\r\n    params[1][0] = \"Test2\";\r\n    params[1][1] = \"Blah2\";\r\n    final List<Object> generatedKeys = runner.insertBatch(\"INSERT INTO blah(col1, col2) VALUES(?,?)\", handler, params);\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    Assert.assertEquals(2, generatedKeys.size());\r\n}"
        }
      },
      {
        "code": "verify(prepStmt, times(2)).addBatch();",
        "line": 740,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodBatchInsert",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodBatchInsert() throws Exception {\r\n    results = mock(ResultSet.class);\r\n    resultsMeta = mock(ResultSetMetaData.class);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);\r\n    when(prepStmt.getGeneratedKeys()).thenReturn(results);\r\n    when(results.next()).thenReturn(true).thenReturn(true).thenReturn(false);\r\n    final ResultSetHandler<List<Object>> handler = rs -> {\r\n        final List<Object> objects = new ArrayList<>();\r\n        while (rs.next()) {\r\n            objects.add(new Object());\r\n        }\r\n        return objects;\r\n    };\r\n    final Object[][] params = new Object[2][2];\r\n    params[0][0] = \"Test\";\r\n    params[0][1] = \"Blah\";\r\n    params[1][0] = \"Test2\";\r\n    params[1][1] = \"Blah2\";\r\n    final List<Object> generatedKeys = runner.insertBatch(\"INSERT INTO blah(col1, col2) VALUES(?,?)\", handler, params);\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    Assert.assertEquals(2, generatedKeys.size());\r\n}"
        }
      },
      {
        "code": "verify(prepStmt, times(1)).executeBatch();",
        "line": 741,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodBatchInsert",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodBatchInsert() throws Exception {\r\n    results = mock(ResultSet.class);\r\n    resultsMeta = mock(ResultSetMetaData.class);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);\r\n    when(prepStmt.getGeneratedKeys()).thenReturn(results);\r\n    when(results.next()).thenReturn(true).thenReturn(true).thenReturn(false);\r\n    final ResultSetHandler<List<Object>> handler = rs -> {\r\n        final List<Object> objects = new ArrayList<>();\r\n        while (rs.next()) {\r\n            objects.add(new Object());\r\n        }\r\n        return objects;\r\n    };\r\n    final Object[][] params = new Object[2][2];\r\n    params[0][0] = \"Test\";\r\n    params[0][1] = \"Blah\";\r\n    params[1][0] = \"Test2\";\r\n    params[1][1] = \"Blah2\";\r\n    final List<Object> generatedKeys = runner.insertBatch(\"INSERT INTO blah(col1, col2) VALUES(?,?)\", handler, params);\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    Assert.assertEquals(2, generatedKeys.size());\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(prepStmt, times(1)).close();",
        "line": 742,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodBatchInsert",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodBatchInsert() throws Exception {\r\n    results = mock(ResultSet.class);\r\n    resultsMeta = mock(ResultSetMetaData.class);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);\r\n    when(prepStmt.getGeneratedKeys()).thenReturn(results);\r\n    when(results.next()).thenReturn(true).thenReturn(true).thenReturn(false);\r\n    final ResultSetHandler<List<Object>> handler = rs -> {\r\n        final List<Object> objects = new ArrayList<>();\r\n        while (rs.next()) {\r\n            objects.add(new Object());\r\n        }\r\n        return objects;\r\n    };\r\n    final Object[][] params = new Object[2][2];\r\n    params[0][0] = \"Test\";\r\n    params[0][1] = \"Blah\";\r\n    params[1][0] = \"Test2\";\r\n    params[1][1] = \"Blah2\";\r\n    final List<Object> generatedKeys = runner.insertBatch(\"INSERT INTO blah(col1, col2) VALUES(?,?)\", handler, params);\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    Assert.assertEquals(2, generatedKeys.size());\r\n}"
        }
      },
      {
        "code": "when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);",
        "line": 795,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodInsert",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodInsert() throws Exception {\r\n    results = mock(ResultSet.class);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);\r\n    when(prepStmt.getGeneratedKeys()).thenReturn(results);\r\n    when(results.next()).thenReturn(true).thenReturn(false);\r\n    when(results.getObject(1)).thenReturn(1L);\r\n    final Long generatedKey = runner.insert(\"INSERT INTO blah(col1, col2) VALUES(?,?)\", new ScalarHandler<>(), \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    Assert.assertEquals(1L, generatedKey.longValue());\r\n}"
        }
      },
      {
        "code": "when(prepStmt.getGeneratedKeys()).thenReturn(results);",
        "line": 796,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodInsert",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodInsert() throws Exception {\r\n    results = mock(ResultSet.class);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);\r\n    when(prepStmt.getGeneratedKeys()).thenReturn(results);\r\n    when(results.next()).thenReturn(true).thenReturn(false);\r\n    when(results.getObject(1)).thenReturn(1L);\r\n    final Long generatedKey = runner.insert(\"INSERT INTO blah(col1, col2) VALUES(?,?)\", new ScalarHandler<>(), \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    Assert.assertEquals(1L, generatedKey.longValue());\r\n}"
        }
      },
      {
        "code": "verify(prepStmt, times(1)).executeUpdate();",
        "line": 802,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodInsert",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodInsert() throws Exception {\r\n    results = mock(ResultSet.class);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);\r\n    when(prepStmt.getGeneratedKeys()).thenReturn(results);\r\n    when(results.next()).thenReturn(true).thenReturn(false);\r\n    when(results.getObject(1)).thenReturn(1L);\r\n    final Long generatedKey = runner.insert(\"INSERT INTO blah(col1, col2) VALUES(?,?)\", new ScalarHandler<>(), \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    Assert.assertEquals(1L, generatedKey.longValue());\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(prepStmt, times(1)).close();",
        "line": 803,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodInsert",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodInsert() throws Exception {\r\n    results = mock(ResultSet.class);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);\r\n    when(prepStmt.getGeneratedKeys()).thenReturn(results);\r\n    when(results.next()).thenReturn(true).thenReturn(false);\r\n    when(results.getObject(1)).thenReturn(1L);\r\n    final Long generatedKey = runner.insert(\"INSERT INTO blah(col1, col2) VALUES(?,?)\", new ScalarHandler<>(), \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    Assert.assertEquals(1L, generatedKey.longValue());\r\n}"
        }
      },
      {
        "code": "verify(prepStmt).setFetchDirection(eq(1));",
        "line": 966,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testStatementConfiguration",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testStatementConfiguration() throws Exception {\r\n    final StatementConfiguration stmtConfig = new StatementConfiguration(1, 2, 3, 4, 5);\r\n    final QueryRunner queryRunner = new QueryRunner(stmtConfig);\r\n    queryRunner.prepareStatement(conn, \"select 1\");\r\n    verify(prepStmt).setFetchDirection(eq(1));\r\n    verify(prepStmt).setFetchSize(eq(2));\r\n    verify(prepStmt).setMaxFieldSize(eq(3));\r\n    verify(prepStmt).setMaxRows(eq(4));\r\n    verify(prepStmt).setQueryTimeout(eq(5));\r\n}"
        }
      },
      {
        "code": "verify(prepStmt).setFetchSize(eq(2));",
        "line": 967,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testStatementConfiguration",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testStatementConfiguration() throws Exception {\r\n    final StatementConfiguration stmtConfig = new StatementConfiguration(1, 2, 3, 4, 5);\r\n    final QueryRunner queryRunner = new QueryRunner(stmtConfig);\r\n    queryRunner.prepareStatement(conn, \"select 1\");\r\n    verify(prepStmt).setFetchDirection(eq(1));\r\n    verify(prepStmt).setFetchSize(eq(2));\r\n    verify(prepStmt).setMaxFieldSize(eq(3));\r\n    verify(prepStmt).setMaxRows(eq(4));\r\n    verify(prepStmt).setQueryTimeout(eq(5));\r\n}"
        }
      },
      {
        "code": "verify(prepStmt).setMaxFieldSize(eq(3));",
        "line": 968,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testStatementConfiguration",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testStatementConfiguration() throws Exception {\r\n    final StatementConfiguration stmtConfig = new StatementConfiguration(1, 2, 3, 4, 5);\r\n    final QueryRunner queryRunner = new QueryRunner(stmtConfig);\r\n    queryRunner.prepareStatement(conn, \"select 1\");\r\n    verify(prepStmt).setFetchDirection(eq(1));\r\n    verify(prepStmt).setFetchSize(eq(2));\r\n    verify(prepStmt).setMaxFieldSize(eq(3));\r\n    verify(prepStmt).setMaxRows(eq(4));\r\n    verify(prepStmt).setQueryTimeout(eq(5));\r\n}"
        }
      },
      {
        "code": "verify(prepStmt).setMaxRows(eq(4));",
        "line": 969,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testStatementConfiguration",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testStatementConfiguration() throws Exception {\r\n    final StatementConfiguration stmtConfig = new StatementConfiguration(1, 2, 3, 4, 5);\r\n    final QueryRunner queryRunner = new QueryRunner(stmtConfig);\r\n    queryRunner.prepareStatement(conn, \"select 1\");\r\n    verify(prepStmt).setFetchDirection(eq(1));\r\n    verify(prepStmt).setFetchSize(eq(2));\r\n    verify(prepStmt).setMaxFieldSize(eq(3));\r\n    verify(prepStmt).setMaxRows(eq(4));\r\n    verify(prepStmt).setQueryTimeout(eq(5));\r\n}"
        }
      },
      {
        "code": "verify(prepStmt).setQueryTimeout(eq(5));",
        "line": 970,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testStatementConfiguration",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testStatementConfiguration() throws Exception {\r\n    final StatementConfiguration stmtConfig = new StatementConfiguration(1, 2, 3, 4, 5);\r\n    final QueryRunner queryRunner = new QueryRunner(stmtConfig);\r\n    queryRunner.prepareStatement(conn, \"select 1\");\r\n    verify(prepStmt).setFetchDirection(eq(1));\r\n    verify(prepStmt).setFetchSize(eq(2));\r\n    verify(prepStmt).setMaxFieldSize(eq(3));\r\n    verify(prepStmt).setMaxRows(eq(4));\r\n    verify(prepStmt).setQueryTimeout(eq(5));\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 52,
    "variableName": "stmt",
    "variableType": "Statement",
    "isGlobal": true,
    "mockPatternLevel": 1,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\QueryRunnerTest.java",
      "className": "QueryRunnerTest"
    },
    "statements": [
      {
        "code": "@Mock\r\nStatement stmt;",
        "line": 78,
        "type": "CREATION",
        "locate": "Attribute",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "FieldDeclaration",
          "methodAnnotations": [
            "Mock"
          ]
        }
      },
      {
        "code": "verify(stmt, times(1)).executeQuery(sql);",
        "line": 458,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodQuery() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(sql, handler, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(sql, handler);\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(stmt, times(1)).close();",
        "line": 460,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodQuery() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(sql, handler, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(sql, handler);\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n}"
        }
      },
      {
        "code": "verify(stmt, times(1)).executeQuery(sql);",
        "line": 482,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Query test cases\r\n//\r\nprivate void callGoodQuery(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(conn, sql, handler, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.query(Connection, String, ResultSetHandler<T>, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(conn, sql, handler);\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(stmt, times(1)).close();",
        "line": 484,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Query test cases\r\n//\r\nprivate void callGoodQuery(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(conn, sql, handler, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.query(Connection, String, ResultSetHandler<T>, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(conn, sql, handler);\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(stmt, times(1)).executeUpdate(sql);",
        "line": 503,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(sql, \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(stmt, times(1)).close();",
        "line": 504,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(sql, \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "verify(stmt, times(1)).executeUpdate(sql);",
        "line": 533,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.update(conn, \"update blah set ? = ?\", \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.update(Connection, String, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    final String sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(conn, \"update blah set unit = ?\", \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(stmt, times(1)).close();",
        "line": 534,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.update(conn, \"update blah set ? = ?\", \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.update(Connection, String, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    final String sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(conn, \"update blah set unit = ?\", \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(conn.createStatement()).thenReturn(stmt);",
        "line": 600,
        "type": "REFERENCE",
        "locate": "@Before",
        "isMockRelated": false,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(conn.prepareCall(any(String.class))).thenReturn(call);\r\n    when(call.getParameterMetaData()).thenReturn(meta);\r\n    when(call.getResultSet()).thenReturn(results);\r\n    when(call.getMoreResults()).thenReturn(false);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new QueryRunner(dataSource);\r\n}"
        }
      },
      {
        "code": "when(stmt.executeQuery(any(String.class))).thenReturn(results);",
        "line": 601,
        "type": "STUBBING",
        "locate": "@Before",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(conn.prepareCall(any(String.class))).thenReturn(call);\r\n    when(call.getParameterMetaData()).thenReturn(meta);\r\n    when(call.getResultSet()).thenReturn(results);\r\n    when(call.getMoreResults()).thenReturn(false);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new QueryRunner(dataSource);\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 53,
    "variableName": "call",
    "variableType": "CallableStatement",
    "isGlobal": true,
    "mockPatternLevel": 2,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\QueryRunnerTest.java",
      "className": "QueryRunnerTest"
    },
    "statements": [
      {
        "code": "@Mock\r\nCallableStatement call;",
        "line": 79,
        "type": "CREATION",
        "locate": "Attribute",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "FieldDeclaration",
          "methodAnnotations": [
            "Mock"
          ]
        }
      },
      {
        "code": "when(call.execute()).thenReturn(false);",
        "line": 175,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "when(call.getUpdateCount()).thenReturn(3);",
        "line": 176,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "verify(call, times(1)).execute();",
        "line": 183,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(call, times(1)).close();",
        "line": 184,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "verify(call, times(2)).execute();",
        "line": 193,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(call, times(2)).close();",
        "line": 194,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "when(call.getObject(1)).thenReturn(42);",
        "line": 199,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "verify(call, times(3)).execute();",
        "line": 207,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(call, times(3)).close();",
        "line": 208,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "when(call.getObject(1)).thenReturn(4242);",
        "line": 213,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "verify(call, times(4)).execute();",
        "line": 220,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(call, times(4)).close();",
        "line": 221,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "when(call.getObject(1)).thenReturn(24);",
        "line": 226,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "when(call.getObject(3)).thenReturn(\"out\");",
        "line": 227,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "verify(call, times(5)).execute();",
        "line": 237,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(call, times(5)).close();",
        "line": 238,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "when(call.execute()).thenReturn(false);",
        "line": 247,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(call.getUpdateCount()).thenReturn(3);",
        "line": 248,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(call, times(1)).execute();",
        "line": 255,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(call, times(1)).close();",
        "line": 256,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(call, times(2)).execute();",
        "line": 265,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(call, times(2)).close();",
        "line": 266,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(call.getObject(1)).thenReturn(42);",
        "line": 271,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(call, times(3)).execute();",
        "line": 279,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(call, times(3)).close();",
        "line": 280,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(call.getObject(1)).thenReturn(4242);",
        "line": 285,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(call, times(4)).execute();",
        "line": 292,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(call, times(4)).close();",
        "line": 293,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(call.getObject(1)).thenReturn(24);",
        "line": 298,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(call.getObject(3)).thenReturn(\"out\");",
        "line": 299,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(call, times(5)).execute();",
        "line": 309,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(call, times(5)).close();",
        "line": 310,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(call.execute()).thenReturn(true);",
        "line": 315,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "verify(call, times(1)).execute();",
        "line": 320,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(call, times(1)).close();",
        "line": 322,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "verify(call, times(2)).execute();",
        "line": 329,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(call, times(2)).close();",
        "line": 331,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "when(call.getObject(1)).thenReturn(42);",
        "line": 336,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "verify(call, times(3)).execute();",
        "line": 343,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(call, times(3)).close();",
        "line": 345,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "when(call.getObject(1)).thenReturn(4242);",
        "line": 350,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "verify(call, times(4)).execute();",
        "line": 356,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(call, times(4)).close();",
        "line": 358,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "when(call.getObject(1)).thenReturn(24);",
        "line": 363,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "when(call.getObject(3)).thenReturn(\"out\");",
        "line": 364,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "verify(call, times(5)).execute();",
        "line": 373,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(call, times(5)).close();",
        "line": 375,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "when(call.execute()).thenReturn(true);",
        "line": 380,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(call, times(1)).execute();",
        "line": 385,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(call, times(1)).close();",
        "line": 387,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(call, times(2)).execute();",
        "line": 394,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(call, times(2)).close();",
        "line": 396,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(call.getObject(1)).thenReturn(42);",
        "line": 401,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(call, times(3)).execute();",
        "line": 408,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(call, times(3)).close();",
        "line": 410,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(call.getObject(1)).thenReturn(4242);",
        "line": 415,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(call, times(4)).execute();",
        "line": 421,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(call, times(4)).close();",
        "line": 423,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(call.getObject(1)).thenReturn(24);",
        "line": 428,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(call.getObject(3)).thenReturn(\"out\");",
        "line": 429,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(call, times(5)).execute();",
        "line": 438,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(call, times(5)).close();",
        "line": 440,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(conn.prepareCall(any(String.class))).thenReturn(call);",
        "line": 603,
        "type": "REFERENCE",
        "locate": "@Before",
        "isMockRelated": false,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(conn.prepareCall(any(String.class))).thenReturn(call);\r\n    when(call.getParameterMetaData()).thenReturn(meta);\r\n    when(call.getResultSet()).thenReturn(results);\r\n    when(call.getMoreResults()).thenReturn(false);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new QueryRunner(dataSource);\r\n}"
        }
      },
      {
        "code": "when(call.getParameterMetaData()).thenReturn(meta);",
        "line": 604,
        "type": "STUBBING",
        "locate": "@Before",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(conn.prepareCall(any(String.class))).thenReturn(call);\r\n    when(call.getParameterMetaData()).thenReturn(meta);\r\n    when(call.getResultSet()).thenReturn(results);\r\n    when(call.getMoreResults()).thenReturn(false);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new QueryRunner(dataSource);\r\n}"
        }
      },
      {
        "code": "when(call.getResultSet()).thenReturn(results);",
        "line": 605,
        "type": "STUBBING",
        "locate": "@Before",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(conn.prepareCall(any(String.class))).thenReturn(call);\r\n    when(call.getParameterMetaData()).thenReturn(meta);\r\n    when(call.getResultSet()).thenReturn(results);\r\n    when(call.getMoreResults()).thenReturn(false);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new QueryRunner(dataSource);\r\n}"
        }
      },
      {
        "code": "when(call.getMoreResults()).thenReturn(false);",
        "line": 606,
        "type": "STUBBING",
        "locate": "@Before",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(conn.prepareCall(any(String.class))).thenReturn(call);\r\n    when(call.getParameterMetaData()).thenReturn(meta);\r\n    when(call.getResultSet()).thenReturn(results);\r\n    when(call.getMoreResults()).thenReturn(false);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new QueryRunner(dataSource);\r\n}"
        }
      },
      {
        "code": "when(call.execute()).thenReturn(true);",
        "line": 658,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testExecuteWithMultipleResultSets",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testExecuteWithMultipleResultSets() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(call.getMoreResults()).thenAnswer(new Answer<Boolean>() {\r\n\r\n        int count = 1;\r\n\r\n        @Override\r\n        public Boolean answer(final InvocationOnMock invocation) {\r\n            return ++count <= 3;\r\n        }\r\n    });\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    final List<Object[]> objects = runner.execute(\"{call my_proc()}\", handler);\r\n    Assert.assertEquals(3, objects.size());\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we close the connection\r\n    verify(conn, times(1)).close();\r\n}"
        }
      },
      {
        "code": "when(call.getMoreResults()).thenAnswer(new Answer<Boolean>() {\r\n\r\n    int count = 1;\r\n\r\n    @Override\r\n    public Boolean answer(final InvocationOnMock invocation) {\r\n        return ++count <= 3;\r\n    }\r\n});",
        "line": 659,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testExecuteWithMultipleResultSets",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testExecuteWithMultipleResultSets() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(call.getMoreResults()).thenAnswer(new Answer<Boolean>() {\r\n\r\n        int count = 1;\r\n\r\n        @Override\r\n        public Boolean answer(final InvocationOnMock invocation) {\r\n            return ++count <= 3;\r\n        }\r\n    });\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    final List<Object[]> objects = runner.execute(\"{call my_proc()}\", handler);\r\n    Assert.assertEquals(3, objects.size());\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we close the connection\r\n    verify(conn, times(1)).close();\r\n}"
        }
      },
      {
        "code": "verify(call, times(1)).execute();",
        "line": 672,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testExecuteWithMultipleResultSets",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testExecuteWithMultipleResultSets() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(call.getMoreResults()).thenAnswer(new Answer<Boolean>() {\r\n\r\n        int count = 1;\r\n\r\n        @Override\r\n        public Boolean answer(final InvocationOnMock invocation) {\r\n            return ++count <= 3;\r\n        }\r\n    });\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    final List<Object[]> objects = runner.execute(\"{call my_proc()}\", handler);\r\n    Assert.assertEquals(3, objects.size());\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we close the connection\r\n    verify(conn, times(1)).close();\r\n}"
        }
      },
      {
        "code": "// make sure we closed the statement\r\nverify(call, times(1)).close();",
        "line": 674,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testExecuteWithMultipleResultSets",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testExecuteWithMultipleResultSets() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(call.getMoreResults()).thenAnswer(new Answer<Boolean>() {\r\n\r\n        int count = 1;\r\n\r\n        @Override\r\n        public Boolean answer(final InvocationOnMock invocation) {\r\n            return ++count <= 3;\r\n        }\r\n    });\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    final List<Object[]> objects = runner.execute(\"{call my_proc()}\", handler);\r\n    Assert.assertEquals(3, objects.size());\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we close the connection\r\n    verify(conn, times(1)).close();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 54,
    "variableName": "meta",
    "variableType": "ParameterMetaData",
    "isGlobal": true,
    "mockPatternLevel": 2,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\QueryRunnerTest.java",
      "className": "QueryRunnerTest"
    },
    "statements": [
      {
        "code": "@Mock\r\nParameterMetaData meta;",
        "line": 80,
        "type": "CREATION",
        "locate": "Attribute",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "FieldDeclaration",
          "methodAnnotations": [
            "Mock"
          ]
        }
      },
      {
        "code": "when(meta.getParameterCount()).thenReturn(2);",
        "line": 91,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callBatchWithException",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Batch test cases\r\n//\r\n// helper method for calling batch when an exception is expected\r\nprivate void callBatchWithException(final String sql, final Object[][] params) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    boolean caught = false;\r\n    try {\r\n        runner.batch(sql, params);\r\n        verify(prepStmt, times(1)).getParameterMetaData();\r\n        verify(prepStmt, times(2)).addBatch();\r\n        verify(prepStmt, times(1)).executeBatch();\r\n        // make sure the statement is closed\r\n        verify(prepStmt, times(1)).close();\r\n        // make sure the connection is closed\r\n        verify(conn, times(1)).close();\r\n    } catch (final SQLException e) {\r\n        System.out.println(\"[TEST] The following exception is expected:\");\r\n        System.out.println(e);\r\n        caught = true;\r\n    }\r\n    if (!caught) {\r\n        fail(\"Exception never thrown, but expected\");\r\n    }\r\n}"
        }
      },
      {
        "code": "when(meta.getParameterCount()).thenReturn(2);",
        "line": 149,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodBatch",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodBatch(final Connection conn, final Object[][] params) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.batch(conn, \"select * from blah where ? = ?\", params);\r\n    verify(prepStmt, times(1)).getParameterMetaData();\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.batch(Connection, String, Object[][]) does not close connections\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(meta.getParameterCount()).thenReturn(2);",
        "line": 164,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodBatch",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodBatch(final Object[][] params, final boolean pmdCheck) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.batch(\"select * from blah where ? = ?\", params);\r\n    verify(prepStmt, times(pmdCheck ? 1 : 0)).getParameterMetaData();\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n}"
        }
      },
      {
        "code": "when(meta.getParameterCount()).thenReturn(2);",
        "line": 178,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// call the other variation of query\r\nwhen(meta.getParameterCount()).thenReturn(0);",
        "line": 188,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// Test single OUT parameter\r\nwhen(meta.getParameterCount()).thenReturn(1);",
        "line": 198,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// Test OUT parameters with IN parameters\r\nwhen(meta.getParameterCount()).thenReturn(3);",
        "line": 212,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// Test INOUT parameters\r\nwhen(meta.getParameterCount()).thenReturn(3);",
        "line": 225,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecute() throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(\"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(\"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(\"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(\"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "when(meta.getParameterCount()).thenReturn(2);",
        "line": 250,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// call the other variation of query\r\nwhen(meta.getParameterCount()).thenReturn(0);",
        "line": 260,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// Test single OUT parameter\r\nwhen(meta.getParameterCount()).thenReturn(1);",
        "line": 270,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// Test OUT parameters with IN parameters\r\nwhen(meta.getParameterCount()).thenReturn(3);",
        "line": 284,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// Test INOUT parameters\r\nwhen(meta.getParameterCount()).thenReturn(3);",
        "line": 297,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecute",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Execute tests\r\n//\r\nprivate void callGoodExecute(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(false);\r\n    when(call.getUpdateCount()).thenReturn(3);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    int result = runner.execute(conn, \"{call my_proc(?, ?)}\", \"unit\", \"test\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(1)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    result = runner.execute(conn, \"{call my_proc()}\");\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(2)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    result = runner.execute(conn, \"{?= call my_proc()}\", intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(3)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(4)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    result = runner.execute(conn, \"{?= call my_proc(?, ?)}\", intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    Assert.assertEquals(3, result);\r\n    verify(call, times(5)).execute();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(meta.getParameterCount()).thenReturn(2);",
        "line": 317,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// call the other variation of query\r\nwhen(meta.getParameterCount()).thenReturn(0);",
        "line": 326,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// Test single OUT parameter\r\nwhen(meta.getParameterCount()).thenReturn(1);",
        "line": 335,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// Test OUT parameters with IN parameters\r\nwhen(meta.getParameterCount()).thenReturn(3);",
        "line": 349,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "// Test INOUT parameters\r\nwhen(meta.getParameterCount()).thenReturn(3);",
        "line": 362,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "when(meta.getParameterCount()).thenReturn(2);",
        "line": 382,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// call the other variation of query\r\nwhen(meta.getParameterCount()).thenReturn(0);",
        "line": 391,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// Test single OUT parameter\r\nwhen(meta.getParameterCount()).thenReturn(1);",
        "line": 400,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// Test OUT parameters with IN parameters\r\nwhen(meta.getParameterCount()).thenReturn(3);",
        "line": 414,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// Test INOUT parameters\r\nwhen(meta.getParameterCount()).thenReturn(3);",
        "line": 427,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(meta.getParameterCount()).thenReturn(2);",
        "line": 445,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodQuery() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(sql, handler, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(sql, handler);\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n}"
        }
      },
      {
        "code": "when(meta.getParameterCount()).thenReturn(2);",
        "line": 469,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Query test cases\r\n//\r\nprivate void callGoodQuery(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(conn, sql, handler, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.query(Connection, String, ResultSetHandler<T>, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(conn, sql, handler);\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(meta.getParameterCount()).thenReturn(2);",
        "line": 490,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(sql, \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "// call the other variation\r\nwhen(meta.getParameterCount()).thenReturn(0);",
        "line": 499,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(sql, \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "// call the other variation\r\nwhen(meta.getParameterCount()).thenReturn(1);",
        "line": 508,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodUpdate() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"update blah set ? = ?\";\r\n    runner.update(sql, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    sql = \"update blah set unit = test\";\r\n    runner.update(sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    sql = \"update blah set unit = ?\";\r\n    runner.update(sql, \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(3)).close();\r\n}"
        }
      },
      {
        "code": "when(meta.getParameterCount()).thenReturn(2);",
        "line": 521,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.update(conn, \"update blah set ? = ?\", \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.update(Connection, String, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    final String sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(conn, \"update blah set unit = ?\", \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// call the other variation\r\nwhen(meta.getParameterCount()).thenReturn(0);",
        "line": 529,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.update(conn, \"update blah set ? = ?\", \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.update(Connection, String, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    final String sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(conn, \"update blah set unit = ?\", \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "// call the other variation\r\nwhen(meta.getParameterCount()).thenReturn(1);",
        "line": 538,
        "type": "STUBBING",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodUpdate",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Update test cases\r\n//\r\nprivate void callGoodUpdate(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.update(conn, \"update blah set ? = ?\", \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.update(Connection, String, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    final String sql = \"update blah set unit = test\";\r\n    runner.update(conn, sql);\r\n    verify(stmt, times(1)).executeUpdate(sql);\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n    // call the other variation\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    runner.update(conn, \"update blah set unit = ?\", \"test\");\r\n    verify(prepStmt, times(2)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(2)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(prepStmt.getParameterMetaData()).thenReturn(meta);",
        "line": 597,
        "type": "REFERENCE",
        "locate": "@Before",
        "isMockRelated": false,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(conn.prepareCall(any(String.class))).thenReturn(call);\r\n    when(call.getParameterMetaData()).thenReturn(meta);\r\n    when(call.getResultSet()).thenReturn(results);\r\n    when(call.getMoreResults()).thenReturn(false);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new QueryRunner(dataSource);\r\n}"
        }
      },
      {
        "code": "when(call.getParameterMetaData()).thenReturn(meta);",
        "line": 604,
        "type": "REFERENCE",
        "locate": "@Before",
        "isMockRelated": false,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(conn.prepareCall(any(String.class))).thenReturn(call);\r\n    when(call.getParameterMetaData()).thenReturn(meta);\r\n    when(call.getResultSet()).thenReturn(results);\r\n    when(call.getMoreResults()).thenReturn(false);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new QueryRunner(dataSource);\r\n}"
        }
      },
      {
        "code": "when(meta.getParameterCount()).thenReturn(0);",
        "line": 668,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testExecuteWithMultipleResultSets",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testExecuteWithMultipleResultSets() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(call.getMoreResults()).thenAnswer(new Answer<Boolean>() {\r\n\r\n        int count = 1;\r\n\r\n        @Override\r\n        public Boolean answer(final InvocationOnMock invocation) {\r\n            return ++count <= 3;\r\n        }\r\n    });\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    final List<Object[]> objects = runner.execute(\"{call my_proc()}\", handler);\r\n    Assert.assertEquals(3, objects.size());\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we close the connection\r\n    verify(conn, times(1)).close();\r\n}"
        }
      },
      {
        "code": "when(meta.getParameterCount()).thenReturn(3);",
        "line": 687,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testFillStatementWithBean",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testFillStatementWithBean() throws Exception {\r\n    final MyBean bean = new MyBean();\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    runner.fillStatementWithBean(prepStmt, bean, \"a\", \"b\", \"c\");\r\n}"
        }
      },
      {
        "code": "when(meta.getParameterCount()).thenReturn(2);",
        "line": 718,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodBatchInsert",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodBatchInsert() throws Exception {\r\n    results = mock(ResultSet.class);\r\n    resultsMeta = mock(ResultSetMetaData.class);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);\r\n    when(prepStmt.getGeneratedKeys()).thenReturn(results);\r\n    when(results.next()).thenReturn(true).thenReturn(true).thenReturn(false);\r\n    final ResultSetHandler<List<Object>> handler = rs -> {\r\n        final List<Object> objects = new ArrayList<>();\r\n        while (rs.next()) {\r\n            objects.add(new Object());\r\n        }\r\n        return objects;\r\n    };\r\n    final Object[][] params = new Object[2][2];\r\n    params[0][0] = \"Test\";\r\n    params[0][1] = \"Blah\";\r\n    params[1][0] = \"Test2\";\r\n    params[1][1] = \"Blah2\";\r\n    final List<Object> generatedKeys = runner.insertBatch(\"INSERT INTO blah(col1, col2) VALUES(?,?)\", handler, params);\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    Assert.assertEquals(2, generatedKeys.size());\r\n}"
        }
      },
      {
        "code": "when(meta.getParameterCount()).thenReturn(2);",
        "line": 794,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodInsert",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodInsert() throws Exception {\r\n    results = mock(ResultSet.class);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);\r\n    when(prepStmt.getGeneratedKeys()).thenReturn(results);\r\n    when(results.next()).thenReturn(true).thenReturn(false);\r\n    when(results.getObject(1)).thenReturn(1L);\r\n    final Long generatedKey = runner.insert(\"INSERT INTO blah(col1, col2) VALUES(?,?)\", new ScalarHandler<>(), \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    Assert.assertEquals(1L, generatedKey.longValue());\r\n}"
        }
      },
      {
        "code": "when(meta.getParameterCount()).thenReturn(2);",
        "line": 902,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testNullHandlerExecute",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test(expected = SQLException.class)\r\npublic void testNullHandlerExecute() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\");\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 55,
    "variableName": "results",
    "variableType": "ResultSet",
    "isGlobal": true,
    "mockPatternLevel": 2,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\QueryRunnerTest.java",
      "className": "QueryRunnerTest"
    },
    "statements": [
      {
        "code": "@Mock\r\nResultSet results;",
        "line": 81,
        "type": "CREATION",
        "locate": "Attribute",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "FieldDeclaration",
          "methodAnnotations": [
            "Mock"
          ]
        }
      },
      {
        "code": "verify(results, times(1)).close();",
        "line": 321,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "verify(results, times(2)).close();",
        "line": 330,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "verify(results, times(3)).close();",
        "line": 344,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "verify(results, times(4)).close();",
        "line": 357,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "verify(results, times(5)).close();",
        "line": 374,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(\"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(\"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(2)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(\"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(3)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(4)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(\"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(5)).close();\r\n}"
        }
      },
      {
        "code": "verify(results, times(1)).close();",
        "line": 386,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(results, times(2)).close();",
        "line": 395,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(results, times(3)).close();",
        "line": 409,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(results, times(4)).close();",
        "line": 422,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(results, times(5)).close();",
        "line": 439,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodExecuteWithResultSet",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodExecuteWithResultSet(final Connection conn) throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    runner.execute(conn, \"{call my_proc(?, ?)}\", handler, \"unit\", \"test\");\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    runner.execute(conn, \"{call my_proc()}\", handler);\r\n    verify(call, times(2)).execute();\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(2)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test single OUT parameter\r\n    when(meta.getParameterCount()).thenReturn(1);\r\n    when(call.getObject(1)).thenReturn(42);\r\n    final OutParameter<Integer> intParam = new OutParameter<>(Types.INTEGER, Integer.class);\r\n    runner.execute(conn, \"{?= call my_proc()}\", handler, intParam);\r\n    Assert.assertEquals(42, intParam.getValue().intValue());\r\n    verify(call, times(3)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(3)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test OUT parameters with IN parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(4242);\r\n    intParam.setValue(null);\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"unit\", \"test\");\r\n    Assert.assertEquals(4242, intParam.getValue().intValue());\r\n    verify(call, times(4)).execute();\r\n    verify(results, times(4)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(4)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n    // Test INOUT parameters\r\n    when(meta.getParameterCount()).thenReturn(3);\r\n    when(call.getObject(1)).thenReturn(24);\r\n    when(call.getObject(3)).thenReturn(\"out\");\r\n    intParam.setValue(null);\r\n    final OutParameter<String> stringParam = new OutParameter<>(Types.VARCHAR, String.class, \"in\");\r\n    runner.execute(conn, \"{?= call my_proc(?, ?)}\", handler, intParam, \"test\", stringParam);\r\n    Assert.assertEquals(24, intParam.getValue().intValue());\r\n    Assert.assertEquals(\"out\", stringParam.getValue());\r\n    verify(call, times(5)).execute();\r\n    verify(results, times(5)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(5)).close();\r\n    // make sure we do not close the connection\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(results, times(1)).close();",
        "line": 450,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodQuery() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(sql, handler, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(sql, handler);\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n}"
        }
      },
      {
        "code": "verify(results, times(2)).close();",
        "line": 459,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "private void callGoodQuery() throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(sql, handler, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(sql, handler);\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(2)).close();\r\n}"
        }
      },
      {
        "code": "verify(results, times(1)).close();",
        "line": 474,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Query test cases\r\n//\r\nprivate void callGoodQuery(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(conn, sql, handler, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.query(Connection, String, ResultSetHandler<T>, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(conn, sql, handler);\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "verify(results, times(2)).close();",
        "line": 483,
        "type": "VERIFICATION",
        "locate": "Other Methods",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "callGoodQuery",
          "methodAnnotations": [],
          "methodRawCode": "//\r\n// Query test cases\r\n//\r\nprivate void callGoodQuery(final Connection conn) throws Exception {\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    String sql = \"select * from blah where ? = ?\";\r\n    runner.query(conn, sql, handler, \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeQuery();\r\n    verify(results, times(1)).close();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we do not close the connection, since QueryRunner.query(Connection, String, ResultSetHandler<T>, Object...) does not close connections\r\n    verify(conn, times(0)).close();\r\n    // call the other variation of query\r\n    sql = \"select * from blah\";\r\n    runner.query(conn, sql, handler);\r\n    verify(stmt, times(1)).executeQuery(sql);\r\n    verify(results, times(2)).close();\r\n    // make sure we closed the statement\r\n    verify(stmt, times(1)).close();\r\n    // make sure we do not close the connection, see above\r\n    verify(conn, times(0)).close();\r\n}"
        }
      },
      {
        "code": "when(prepStmt.executeQuery()).thenReturn(results);",
        "line": 598,
        "type": "REFERENCE",
        "locate": "@Before",
        "isMockRelated": false,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(conn.prepareCall(any(String.class))).thenReturn(call);\r\n    when(call.getParameterMetaData()).thenReturn(meta);\r\n    when(call.getResultSet()).thenReturn(results);\r\n    when(call.getMoreResults()).thenReturn(false);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new QueryRunner(dataSource);\r\n}"
        }
      },
      {
        "code": "when(stmt.executeQuery(any(String.class))).thenReturn(results);",
        "line": 601,
        "type": "REFERENCE",
        "locate": "@Before",
        "isMockRelated": false,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(conn.prepareCall(any(String.class))).thenReturn(call);\r\n    when(call.getParameterMetaData()).thenReturn(meta);\r\n    when(call.getResultSet()).thenReturn(results);\r\n    when(call.getMoreResults()).thenReturn(false);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new QueryRunner(dataSource);\r\n}"
        }
      },
      {
        "code": "when(call.getResultSet()).thenReturn(results);",
        "line": 605,
        "type": "REFERENCE",
        "locate": "@Before",
        "isMockRelated": false,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(conn.prepareCall(any(String.class))).thenReturn(call);\r\n    when(call.getParameterMetaData()).thenReturn(meta);\r\n    when(call.getResultSet()).thenReturn(results);\r\n    when(call.getMoreResults()).thenReturn(false);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new QueryRunner(dataSource);\r\n}"
        }
      },
      {
        "code": "when(results.next()).thenReturn(false);",
        "line": 608,
        "type": "STUBBING",
        "locate": "@Before",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "setUp",
          "methodAnnotations": [
            "Before"
          ],
          "methodRawCode": "@Before\r\npublic void setUp() throws Exception {\r\n    when(dataSource.getConnection()).thenReturn(conn);\r\n    when(conn.prepareStatement(any(String.class))).thenReturn(prepStmt);\r\n    when(prepStmt.getParameterMetaData()).thenReturn(meta);\r\n    when(prepStmt.executeQuery()).thenReturn(results);\r\n    when(conn.createStatement()).thenReturn(stmt);\r\n    when(stmt.executeQuery(any(String.class))).thenReturn(results);\r\n    when(conn.prepareCall(any(String.class))).thenReturn(call);\r\n    when(call.getParameterMetaData()).thenReturn(meta);\r\n    when(call.getResultSet()).thenReturn(results);\r\n    when(call.getMoreResults()).thenReturn(false);\r\n    when(results.next()).thenReturn(false);\r\n    handler = new ArrayHandler();\r\n    runner = new QueryRunner(dataSource);\r\n}"
        }
      },
      {
        "code": "verify(results, times(3)).close();",
        "line": 673,
        "type": "VERIFICATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testExecuteWithMultipleResultSets",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testExecuteWithMultipleResultSets() throws Exception {\r\n    when(call.execute()).thenReturn(true);\r\n    when(call.getMoreResults()).thenAnswer(new Answer<Boolean>() {\r\n\r\n        int count = 1;\r\n\r\n        @Override\r\n        public Boolean answer(final InvocationOnMock invocation) {\r\n            return ++count <= 3;\r\n        }\r\n    });\r\n    when(meta.getParameterCount()).thenReturn(0);\r\n    final List<Object[]> objects = runner.execute(\"{call my_proc()}\", handler);\r\n    Assert.assertEquals(3, objects.size());\r\n    verify(call, times(1)).execute();\r\n    verify(results, times(3)).close();\r\n    // make sure we closed the statement\r\n    verify(call, times(1)).close();\r\n    // make sure we close the connection\r\n    verify(conn, times(1)).close();\r\n}"
        }
      },
      {
        "code": "results = mock(ResultSet.class);",
        "line": 715,
        "type": "CREATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodBatchInsert",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodBatchInsert() throws Exception {\r\n    results = mock(ResultSet.class);\r\n    resultsMeta = mock(ResultSetMetaData.class);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);\r\n    when(prepStmt.getGeneratedKeys()).thenReturn(results);\r\n    when(results.next()).thenReturn(true).thenReturn(true).thenReturn(false);\r\n    final ResultSetHandler<List<Object>> handler = rs -> {\r\n        final List<Object> objects = new ArrayList<>();\r\n        while (rs.next()) {\r\n            objects.add(new Object());\r\n        }\r\n        return objects;\r\n    };\r\n    final Object[][] params = new Object[2][2];\r\n    params[0][0] = \"Test\";\r\n    params[0][1] = \"Blah\";\r\n    params[1][0] = \"Test2\";\r\n    params[1][1] = \"Blah2\";\r\n    final List<Object> generatedKeys = runner.insertBatch(\"INSERT INTO blah(col1, col2) VALUES(?,?)\", handler, params);\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    Assert.assertEquals(2, generatedKeys.size());\r\n}"
        }
      },
      {
        "code": "when(prepStmt.getGeneratedKeys()).thenReturn(results);",
        "line": 720,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodBatchInsert",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodBatchInsert() throws Exception {\r\n    results = mock(ResultSet.class);\r\n    resultsMeta = mock(ResultSetMetaData.class);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);\r\n    when(prepStmt.getGeneratedKeys()).thenReturn(results);\r\n    when(results.next()).thenReturn(true).thenReturn(true).thenReturn(false);\r\n    final ResultSetHandler<List<Object>> handler = rs -> {\r\n        final List<Object> objects = new ArrayList<>();\r\n        while (rs.next()) {\r\n            objects.add(new Object());\r\n        }\r\n        return objects;\r\n    };\r\n    final Object[][] params = new Object[2][2];\r\n    params[0][0] = \"Test\";\r\n    params[0][1] = \"Blah\";\r\n    params[1][0] = \"Test2\";\r\n    params[1][1] = \"Blah2\";\r\n    final List<Object> generatedKeys = runner.insertBatch(\"INSERT INTO blah(col1, col2) VALUES(?,?)\", handler, params);\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    Assert.assertEquals(2, generatedKeys.size());\r\n}"
        }
      },
      {
        "code": "when(results.next()).thenReturn(true).thenReturn(true).thenReturn(false);",
        "line": 721,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodBatchInsert",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodBatchInsert() throws Exception {\r\n    results = mock(ResultSet.class);\r\n    resultsMeta = mock(ResultSetMetaData.class);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);\r\n    when(prepStmt.getGeneratedKeys()).thenReturn(results);\r\n    when(results.next()).thenReturn(true).thenReturn(true).thenReturn(false);\r\n    final ResultSetHandler<List<Object>> handler = rs -> {\r\n        final List<Object> objects = new ArrayList<>();\r\n        while (rs.next()) {\r\n            objects.add(new Object());\r\n        }\r\n        return objects;\r\n    };\r\n    final Object[][] params = new Object[2][2];\r\n    params[0][0] = \"Test\";\r\n    params[0][1] = \"Blah\";\r\n    params[1][0] = \"Test2\";\r\n    params[1][1] = \"Blah2\";\r\n    final List<Object> generatedKeys = runner.insertBatch(\"INSERT INTO blah(col1, col2) VALUES(?,?)\", handler, params);\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    Assert.assertEquals(2, generatedKeys.size());\r\n}"
        }
      },
      {
        "code": "results = mock(ResultSet.class);",
        "line": 792,
        "type": "CREATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodInsert",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodInsert() throws Exception {\r\n    results = mock(ResultSet.class);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);\r\n    when(prepStmt.getGeneratedKeys()).thenReturn(results);\r\n    when(results.next()).thenReturn(true).thenReturn(false);\r\n    when(results.getObject(1)).thenReturn(1L);\r\n    final Long generatedKey = runner.insert(\"INSERT INTO blah(col1, col2) VALUES(?,?)\", new ScalarHandler<>(), \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    Assert.assertEquals(1L, generatedKey.longValue());\r\n}"
        }
      },
      {
        "code": "when(prepStmt.getGeneratedKeys()).thenReturn(results);",
        "line": 796,
        "type": "REFERENCE",
        "locate": "Test Case",
        "isMockRelated": false,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodInsert",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodInsert() throws Exception {\r\n    results = mock(ResultSet.class);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);\r\n    when(prepStmt.getGeneratedKeys()).thenReturn(results);\r\n    when(results.next()).thenReturn(true).thenReturn(false);\r\n    when(results.getObject(1)).thenReturn(1L);\r\n    final Long generatedKey = runner.insert(\"INSERT INTO blah(col1, col2) VALUES(?,?)\", new ScalarHandler<>(), \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    Assert.assertEquals(1L, generatedKey.longValue());\r\n}"
        }
      },
      {
        "code": "when(results.next()).thenReturn(true).thenReturn(false);",
        "line": 797,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodInsert",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodInsert() throws Exception {\r\n    results = mock(ResultSet.class);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);\r\n    when(prepStmt.getGeneratedKeys()).thenReturn(results);\r\n    when(results.next()).thenReturn(true).thenReturn(false);\r\n    when(results.getObject(1)).thenReturn(1L);\r\n    final Long generatedKey = runner.insert(\"INSERT INTO blah(col1, col2) VALUES(?,?)\", new ScalarHandler<>(), \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    Assert.assertEquals(1L, generatedKey.longValue());\r\n}"
        }
      },
      {
        "code": "when(results.getObject(1)).thenReturn(1L);",
        "line": 798,
        "type": "STUBBING",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodInsert",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodInsert() throws Exception {\r\n    results = mock(ResultSet.class);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);\r\n    when(prepStmt.getGeneratedKeys()).thenReturn(results);\r\n    when(results.next()).thenReturn(true).thenReturn(false);\r\n    when(results.getObject(1)).thenReturn(1L);\r\n    final Long generatedKey = runner.insert(\"INSERT INTO blah(col1, col2) VALUES(?,?)\", new ScalarHandler<>(), \"unit\", \"test\");\r\n    verify(prepStmt, times(1)).executeUpdate();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    Assert.assertEquals(1L, generatedKey.longValue());\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 56,
    "variableName": "resultsMeta",
    "variableType": "ResultSetMetaData",
    "isGlobal": true,
    "mockPatternLevel": 1,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\QueryRunnerTest.java",
      "className": "QueryRunnerTest"
    },
    "statements": [
      {
        "code": "@Mock\r\nResultSetMetaData resultsMeta;",
        "line": 83,
        "type": "CREATION",
        "locate": "Attribute",
        "isMockRelated": true,
        "isShareable": true,
        "locationContext": {
          "methodName": "FieldDeclaration",
          "methodAnnotations": [
            "Mock"
          ]
        }
      },
      {
        "code": "resultsMeta = mock(ResultSetMetaData.class);",
        "line": 716,
        "type": "CREATION",
        "locate": "Test Case",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testGoodBatchInsert",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testGoodBatchInsert() throws Exception {\r\n    results = mock(ResultSet.class);\r\n    resultsMeta = mock(ResultSetMetaData.class);\r\n    when(meta.getParameterCount()).thenReturn(2);\r\n    when(conn.prepareStatement(any(String.class), eq(Statement.RETURN_GENERATED_KEYS))).thenReturn(prepStmt);\r\n    when(prepStmt.getGeneratedKeys()).thenReturn(results);\r\n    when(results.next()).thenReturn(true).thenReturn(true).thenReturn(false);\r\n    final ResultSetHandler<List<Object>> handler = rs -> {\r\n        final List<Object> objects = new ArrayList<>();\r\n        while (rs.next()) {\r\n            objects.add(new Object());\r\n        }\r\n        return objects;\r\n    };\r\n    final Object[][] params = new Object[2][2];\r\n    params[0][0] = \"Test\";\r\n    params[0][1] = \"Blah\";\r\n    params[1][0] = \"Test2\";\r\n    params[1][1] = \"Blah2\";\r\n    final List<Object> generatedKeys = runner.insertBatch(\"INSERT INTO blah(col1, col2) VALUES(?,?)\", handler, params);\r\n    verify(prepStmt, times(2)).addBatch();\r\n    verify(prepStmt, times(1)).executeBatch();\r\n    // make sure we closed the statement\r\n    verify(prepStmt, times(1)).close();\r\n    // make sure we closed the connection\r\n    verify(conn, times(1)).close();\r\n    Assert.assertEquals(2, generatedKeys.size());\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 57,
    "variableName": "resultSet",
    "variableType": "ResultSet",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\ResultSetIteratorTest.java"
    },
    "statements": [
      {
        "code": "final ResultSet resultSet = mock(ResultSet.class);",
        "line": 35,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testCreatesResultSetIteratorTakingThreeArgumentsAndCallsRemove",
          "methodAnnotations": [
            "Test"
          ],
          "methodRawCode": "@Test\r\npublic void testCreatesResultSetIteratorTakingThreeArgumentsAndCallsRemove() {\r\n    final ResultSet resultSet = mock(ResultSet.class);\r\n    final ResultSetIterator resultSetIterator = new ResultSetIterator(resultSet, null);\r\n    resultSetIterator.remove();\r\n}"
        }
      }
    ]
  },
  {
    "rawMockObjectId": 58,
    "variableName": "wrappedRs",
    "variableType": "ResultSet",
    "isGlobal": false,
    "mockPatternLevel": 0,
    "classContext": {
      "packageName": "org.apache.commons.dbutils.wrappers",
      "filePath": "C:\\Java_projects\\temp_projects\\Commons-DbUtils\\src\\test\\java\\org\\apache\\commons\\dbutils\\wrappers\\SqlNullCheckedResultSetTest.java"
    },
    "statements": [
      {
        "code": "final ResultSet wrappedRs = mock(ResultSet.class);",
        "line": 984,
        "type": "CREATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testWrapResultSet",
          "methodAnnotations": [],
          "methodRawCode": "public void testWrapResultSet() throws SQLException {\r\n    final ResultSet wrappedRs = mock(ResultSet.class);\r\n    final ResultSet rs = SqlNullCheckedResultSet.wrap(wrappedRs);\r\n    rs.beforeFirst();\r\n    verify(wrappedRs).beforeFirst();\r\n    rs.next();\r\n    verify(wrappedRs).next();\r\n}"
        }
      },
      {
        "code": "verify(wrappedRs).beforeFirst();",
        "line": 987,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testWrapResultSet",
          "methodAnnotations": [],
          "methodRawCode": "public void testWrapResultSet() throws SQLException {\r\n    final ResultSet wrappedRs = mock(ResultSet.class);\r\n    final ResultSet rs = SqlNullCheckedResultSet.wrap(wrappedRs);\r\n    rs.beforeFirst();\r\n    verify(wrappedRs).beforeFirst();\r\n    rs.next();\r\n    verify(wrappedRs).next();\r\n}"
        }
      },
      {
        "code": "verify(wrappedRs).next();",
        "line": 989,
        "type": "VERIFICATION",
        "locate": "",
        "isMockRelated": true,
        "isShareable": false,
        "locationContext": {
          "methodName": "testWrapResultSet",
          "methodAnnotations": [],
          "methodRawCode": "public void testWrapResultSet() throws SQLException {\r\n    final ResultSet wrappedRs = mock(ResultSet.class);\r\n    final ResultSet rs = SqlNullCheckedResultSet.wrap(wrappedRs);\r\n    rs.beforeFirst();\r\n    verify(wrappedRs).beforeFirst();\r\n    rs.next();\r\n    verify(wrappedRs).next();\r\n}"
        }
      }
    ]
  }
]